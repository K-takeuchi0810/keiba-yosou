# 予想ロジック分析官 採点 — Phase 0.5-4B 基盤修復 (commit `99eec68`)

## 判定: HOLD

**理由**: type-B。365 日ローリングの実装は **実データ照合 1,200 行で不一致 0** と正しく、前回指摘の域外外挿・docs の skew 逆記述・基準市場の二分はすべて是正済み。ただし新設 `h_history_truncated` が **(i) 発火の 17.5% が誤検出**、**(ii) 学習行 40.9% → 運用行 1.8% と時間代理変数そのもの**になっており、0.5-5 のベースラインとして凍結する前に定義を差し替えるべき。是正はデータから直接可能なので軽い不足 = HOLD。

## 総合: 3.6 / 5 (前回 3.3)

## 依頼 5 点への回答 (`build_dataset("20220101","20260831")` = 222,491 行を再構築)

**1. 365 日ローリングの実装 → 正しい。** audit は調教師しか照合していないので **騎手** `j_rides_365` を DB から独立に数え直した (2026 窓 600 行 + 2022 上半期 600 行)。**不一致 0 / 1,200**。境界は `dq[0] < cutoff` なので「ちょうど 365 日前」を含む。境界を `> lo` に変えると 259/600 が不一致になるので、一致は偶然ではなく定義どおり。deque は `ORDER BY d, start_time` 順に append されるため単調で popleft が成立する。

**2. `h_history_truncated` の定義 → 取りこぼし 0、誤検出 17.5%。しかも真値が DB から直接取れる。**

| 初出 2021 の年齢 | 頭数 | 2020 行あり | 現行フラグ |
|---|---|---|---|
| 2 歳 | 3,296 | 0.0% | 0 (正) |
| 3 歳 | 4,344 | **69.5%** | 1 → **30.5% = 1,326 頭が誤検出** |
| 4 歳 | 1,631 | 98.3% | 1 |
| 5 歳+ | 2,290 | 95.9% | 1 |

混同行列: (flag=1, truth=1) 6,817 / (flag=1, truth=0) **1,448** / (flag=0, truth=1) **0**。再現率 100%、精度 82.5%。

**より重要**: フラグの行比率が 2022 40.9% → 2026 1.8%。**学習期間内で 4 倍動く時間代理変数**であり、本改修が潰そうとした欠陥クラスを別名で 1 つ増やしている。単変量 AUC も 0.468 (2022) → 0.494 (2026) と意味が動く。是正は「`race_year < TRUST_FLOOR_YEAR` の行に同じ血統番号があるか」で定義するだけ。

**3. 率を累積のまま残した判断 → 欠陥ではなく許容範囲。ただし設計の不整合として記録し、変えるなら今。** 分母は 5.0 倍になるが `_rate` の事前分布 (pn=20) は両時点で無視できる。分布は j_winrate sd 0.0463 → 0.0449 と微小、単変量 AUC は 0.675 / 0.669 / 0.673 / 0.679 / 0.678 と定常。問題は **カウントだけ窓を切り勝ち数を渡していない**こと: モデルは「直近 1 年の騎乗数」は見えるが「直近 1 年の勝率」を組めない。**0.5-5 事前登録の再凍結前に決めて以後動かさない**のが規律。

**4. `j_rides_365` の 2.05% 域外 → clip しない判断は妥当。ただし理由は「隠さない」より「木では clip が予測に無効」。** 評価窓 325 行 (騎手 2 名)。学習に ≥900 の行が 2,862 行あり上端は密。LightGBM は学習 max 超を最終 bin に落とすので **945 で clip しても予測は 1 行も変わらない**。clip の有無は監査指標の正直さにしか影響しない。

**5. 検証窓半減の読み方 → best_iteration は学習窓も変わったので直接比較不能。** fundamental 192 → 247 は train も valid も動いた結果で「増えた」に意味を与えるべきでない。offset の 27 (不変) は Δ = 0.00012 を 47,715 行で判定しており、前回 94,709 行で出した CI より広い区間になる (今回は未算出)。early stopping が 27 で止まる = 確定オッズに足せる残差が無い、という前回結論の再現。

## 項目別

- **シグナル網羅性: 3/5** — 30→31 特徴、全件が馬柱に載る市場代理変数で前回と同じ
- **重み妥当性 / 過適合リスク: 4/5** (前回 3.5) — 窓 365 日はデータ構造のみから決定、2026 不接触、新テスト 4 件が元実装で落ちることを確認済み。減点: フラグの時間代理性
- **信頼度判定 / 確率推定の構造: 4/5** — 前回「補正が T−10 の FLB を強める向き (corr −0.179)」と指摘した歪みは **corr(margin, logit P_T10) = +0.038** に解消。**域外外挿の産物だったという前回の推定が裏付けられた**。margin sd 0.0807 → 0.0721 と補正量も縮小
- **デッドコード / 設計の整合性: 3.5/5** — `SPLIT_DIVERGENCE` は良い契約。減点: カウント窓化 / 率累積の非対称、真値が取れるのにヒューリスティクス、`fit()` docstring が古いまま
- **train-serve skew: 3.5/5** (前回 2.5) — 域外率の是正・docs の訂正・`payout_agreement` の刻印・基準市場の二分を確認。残: フラグ意味の drift、累積率の分母 5 倍

## 停止条件チェック

- [x] git_sha / dirty_paths 記録あり
- [x] baseline paired 比較成立 — 同一 626 レース / 8,279 頭を再現 (top1==pop1 97.12%、mean|edge| 0.284pt、max 3.739pt を再導出)
- [x] market_snapshot / payout — N/A
- [x] 専門領域別 — over-claim 無し

## 反証の試み

1. 「ローリング実装は正しい」→ 別の主体 (騎手) を別の乱数で 1,200 行 SQL 照合、不一致 0。境界を 1 日ずらすと 43% が不一致 → **成立**
2. 「h_history_truncated は途切れた馬に印を付ける」→ 取りこぼし 0 だが **1,448 頭 (17.5%) を誤検出**、比率が時間代理 → **部分的に不成立**
3. 「結論は変わらない (artifact でなかった)」→ margin と市場価格の逆相関が消えているので、**前回の負の結果のうち「補正が FLB を強めた」部分は artifact だった**。ただし β₂・LogLoss・候補 0 頭は再現 → 主結論は **成立**、副次記述の一部は修正が必要

## 主な改善提案

1. **フラグをデータ定義に** — 年齢ルールを廃し、`race_year < TRUST_FLOOR_YEAR` の登録番号集合との照合に。テスト: 「2021 に 3 歳初出で 2020 行なしの馬は 0」
2. **率の窓化を事前登録前に決める** — `j_wins_365` を足して `_rate(wins_365, rides_365)` にするか現状維持を明記。どちらでも 2026 を見ずに決め、基礎特徴表に固定
3. **offset の validation Δ に CI** — best_iteration=27 が「Δ の CI が 0 をまたぐ」と併記されるように

## 前回からの差分 (前回 HOLD 3.3)

- シグナル網羅性 3 → 3 / 重み妥当性 3.5 → 4 (+0.5) / 確率推定 4 → 4 / 設計整合性 3.5 → 3.5 / train-serve skew 2.5 → 3.5 (+1.0、前回提案 1・2・3 すべて実施、FLB 逆押しの解消を実測で確認)
- 判定 HOLD → HOLD。前回 HOLD 事由は解消。今回の事由は新設フラグの誤検出 17.5% と時間代理性で、0.5-5 ベースライン凍結前に是正すべき 1 件
