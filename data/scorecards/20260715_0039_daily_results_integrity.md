# 採点 2026-07-15 00:39

**改修内容**: 日次結果の朝オッズ/人気分離、馬番00除外、race_num統一と再生成検証
**対象ファイル**: `scripts/build_daily_results.py`, `tests/test_build_daily_results.py`, `data/results/2026-07-12/*.csv`, `data/results/2026-07-12/manifest.json`, `data/results/2026-07-12/features_00_contamination.md`

> 注: `expert-review` が参照する旧 `.Codex/agents/_rubric.md` と役割別mdは現リポジトリに存在しないため、7専門家は一般的な5段階基準（5=卓越、4=良好、3=実用、2=重大課題、1=破綻）で評価した。

## 総合スコア (項目平均) の推移

| 専門家 | 今回 | 前回 | 差分 | 判定 |
|---|---:|---:|---:|---|
| GUI / UX 監査人 | 4.1 | 3.6 | +0.5 | PASS |
| モバイル HTML レビュアー | 4.8 | 4.6 | +0.2 | PASS |
| 予想ロジック分析官 | 4.2 | 4.0 | +0.2 | PASS |
| 収益性ジャッジ | 4.1 | 3.2 | +0.9 | PASS（戦略収益性の一般化は対象外） |
| データ基盤エンジニア | 4.5 | 3.8 | +0.7 | PASS |
| コード品質レビュアー | 4.2 | 4.0 | +0.2 | PASS |
| 検証プロセス監査人 | 4.2 | 4.3 | -0.1 | PASS |
| **平均** | **4.30** | **3.93** | **+0.37** | **PASS** |

0.3以上の後退はなく、全項目維持または改善（検証プロセスの-0.1は警告閾値未満）。

## 各専門家の所見

### GUI / UX 監査人

- **4.1 / PASS**。GUI自体は無変更だが、日次成果物の信頼性、CSV間の目視・表計算結合性が改善。
- 非空朝人気169件は全件1〜18、馬番00は0件、5 CSVのrace_numは2桁、manifest件数も一致。
- 残存する`odds_fetched_at`欠損137件を生成結果の警告として見せることを推奨。

### モバイル HTML レビュアー

- **4.8 / PASS**。表示HTML/CSSは無変更で、iPhone Safariへの直接回帰なし。
- 実HTMLと同じ`<br> + span.pick-reason`をテストし、`22.9 / 6`、`77.3 / 10`を復元。
- 将来は`data-odds` / `data-popularity`等の意味属性から抽出するとテンプレート変更耐性が上がる。

### 予想ロジック分析官

- **4.2 / PASS**。予想・確率計算は無変更。評価結合の欠損・重複リスクだけを解消。
- `features.py`の19参照は成績または4角条件で馬番00を自然除外し、現状の特徴量汚染0件という結論は妥当。
- 生成時に`morning_odds > 0`と`1 <= popularity <= starter_count`の品質ゲート追加を推奨。

### 収益性ジャッジ

- **4.1 / PASS**。5 CSVの件数、ハッシュ、キー、結合、払戻、損益が一致。
- 当日の購入は1件、投資100円、払戻0円、損益-100円。計算は正しいが収益性の一般化は不可。
- 今回は戦略改善ではなく評価データの信頼性回復。鮮度欠損137件のmanifest警告を推奨。

### データ基盤エンジニア

- **4.5 / PASS**。SQL段階で馬番00を除外し、final_odds/race_resultsを506行から正規470行へ是正。
- manifest件数・SHA256、損益再計算、全テスト、`features.py`汚染調査はいずれも整合。
- 次の別タスクとして取り込み段階で馬番00を拒否し、既存406行をバックアップ付きで整理することを推奨。

### コード品質レビュアー

- **4.2 / PASS**。3修正は局所的で、`race_num_of()`により重複を削減。A/B/Cを直接捕捉する回帰テストあり。
- `" ".join(self._td_buf)`は全tdに作用するため、将来ネストされた馬名等に空白を入れる潜在リスクがある。
- オッズセル専用バッファへの限定と、非オッズセルのネスト回帰テストを推奨。

### 検証プロセス監査人

- **4.2 / PASS（最終ゲート）**。対象3件、全体316件のテスト、実CSV6項目、manifest SHA256を独立照合。
- `morning_popularity`は「全470行」ではなく「非空169行が1〜17、空301行」と表現すべきで、今回の報告はこの条件を明示済み。
- 6項目の実データ検証を恒久的pytestまたは検証スクリプトへ移すことを推奨。

## 横断的に見た優先課題

1. **生成時の品質ゲートとmanifest警告を追加**（予想ロジック + 収益性 + GUI + 検証）
   - 朝オッズ/人気の値域、`odds_fetched_at`欠損137件/11レース、件数・ハッシュを機械的に記録・警告する。
2. **馬番00を取り込み段階で防止し、既存406行を別タスクで整理**（データ基盤）
   - DB削除は今回の対象外。バックアップ・件数照合・回帰テストを伴う専用改修とする。
3. **HTMLパーサを意味属性または専用バッファへ強化**（モバイル + コード品質）
   - 全tdへの区切り挿入ではなく、オッズ/人気要素だけを構造的に分離する。
