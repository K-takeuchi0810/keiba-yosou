# 採点 2026-05-24 14:00 — P19 B1-S0 pre-flight

**改修内容**: Phase B1 着手前の Welch t-test + bootstrap CI + 2026Q1 root cause 5+ 軸 hit_rate 分解の診断 script 2 件追加 + 9 features leak audit doc + Gate-2 三条件 binary 評価 + plan v2.1 patch
**対象ファイル**:
- `scripts/season_brier_test.py` (新規, 263 行, scipy + numpy)
- `scripts/q1_root_cause.py` (新規, 340 行, sqlite3 + csv)
- `docs/phase_b1_leak_audit.md` (新規, 517 行, 9 features SQL audit)
- `data/scorecards/20260524_0900_p19_b1_plan_v2.md` (= plan v2.1 patch, §5.2 multi-fold row)
- `data/scorecards/20260524_1315_p19_b1s0_results.md` (= B1-S0 結果集約)

**手続的記録**: B1-S0 開始 (11:30) から expert-review trigger (13:50) まで 2h20m。本セッションでは予測 pipeline 本体 (`gui/`, `predictor/`, `jvlink_client/`, `web/`) を 1 文字も触っていない、診断 script + scorecard + audit doc + plan v2.1 patch のみ。CLAUDE.md ルール 1 で `scripts/` 配下に新規 .py を追加した場合は expert-review 必須なので 7 並列起動。master CWD で commit 済 (bf9cb99)、ルール 1-bis 該当なし。

---

## 総合スコア (項目平均) の推移

| 専門家 | 今回 | 前回 | 差分 |
|---|---:|---:|---:|
| GUI / UX 監査人 | 3.8 | 3.6 | +0.2 |
| モバイル HTML レビュアー | 4.6 | 4.6 | ±0 |
| 予測ロジック分析家 | 4.0 | 4.1 | -0.1 |
| 収益性判定家 | 3.3 | 3.5 | -0.2 |
| データパイプライン技術者 | 4.2 | 4.4 | -0.2 |
| **コード品質レビュアー** | **3.4** | **4.5** | **-1.1 ⚠ 後退** |
| **検証プロセス監査人** | **3.8** | **4.55** | **-0.75 ⚠ 後退** |

**平均**: 3.87 / 5 (前回 3.86 → ほぼ同等、ただし内訳で 2 名大幅後退 + 1 名小改善 + 4 名小後退の不健康な構造)

**⚠ 警告**: 2 名で前回比 -0.3 以上の後退 (= 0.3 閾値超え)。原因は本セッションで追加した診断 script の品質課題 (DRY 違反 / マジックナンバー / 統計検定設計の power 計算欠落 / 多重検定 Bonferroni 未補正) に集中。本体 module は無編集だが、新規 script の品質が本体 module の構造的勝利を覆い隠す形で集計平均を維持している。

---

## 各専門家の所見

### GUI / UX 監査人 (3.8 / 5, +0.2)

`gui/app.py` 本体は本セッション 1 文字も触っていない (`git diff bf9cb99 -- gui/app.py` 空)、JS 検証 `node --check` PASS (12824 chars, EXIT=0)。前回 1/5 だった「レイアウト / アクセシビリティ」は **本セッション外** (P17 系列の `get_buy_filter_default` API + `restoreOptions` boot sync) で 3/5 まで回復。診断 script 2 件 (`scripts/season_brier_test.py` / `scripts/q1_root_cause.py`) は CLI 専用で **GUI 導線なし**、UX 影響は中立。

**改善提案 1 件 (低優先)**: diagnostic script を GUI Ⅳ-bis として露出。`gui/app.py:1424-1425` の「確認」セクション末尾に `<button data-action="run_diagnose_season_brier">Ⅳ 診断: 月次 Brier 比較</button>` を 1 行追加し、`Api.run_diagnose_season_brier` で subprocess kick + `_set_status` で 1 行サマリ表示。月次運用の参入障壁が PowerShell 知識から GUI ボタン押下に下がる。

---

### モバイル HTML レビュアー (4.6 / 5, ±0)

`git diff bf9cb99 -- web/templates/ web/generator.py --stat` 出力空 = HTML 経路を構成するすべてのファイルが**完全に無改修**。前回 (S7 = 4.6) を維持する以外の判定はできない。生成物 `web/dist/index.html` は前回 S7 ビルドのまま。

**新規提案 1 件 (B1-S1 と並行 30 分)**: H3-3 で「不調が track-specific + small-race に集中」と浮上したことを HTML 出力上で「買い候補ごとに track risk badge を表示」する形で活かす案。`web/generator.py` の `build_view_model` に `small-race-flag` + `track_recent_brier_decile` 計算追加 + `.buy-card` に `<span class="track-risk-pill">小頭数 / Brier-low</span>` 追加。S7 で繰越し中の 3 件 (`summary` padding / `--bg-mute` / `.waku-1` ダーク輝度) と同セッションでバッチ処理が最効率。

---

### 予測ロジック分析家 (4.0 / 5, -0.1)

`predictor/rules.py` / `features.py` / `weights.json` / `calibrator.json` は本セッション完全無編集。ただし `docs/phase_b1_leak_audit.md` で **B1-S1 採用候補 4 features の SQL 案 + leak audit verdict** が確定し、`audit 項目 3 (= 当日 publish 時刻)` を 9 features 全件に明示適用し T2.1a/T2.2b (PARTIAL) / T3.3a (FAIL) を識別したのは本番運用リスクを正面から潰す設計。一方、本セッションで以下 2 件の設計欠落:

1. B1-S1 で 4 features を入れる時の **weights namespace 設計指針が plan に欠落** (track-prior 系 3 つの multicollinearity overfit risk)
2. 新規 T3.2a `track_bias_inside_outside` が既存 `same_day_track_bias` と **命名空間衝突気味**

**改善提案 3 件**:
1. B1-S1 着手前に `weights.json._comment_b1` ブロックで `track_prior` / `pace_estimate` の 2 namespace を分け、既定値 0 で起動 (= LGBM 経由のみ、rules.py 直接加算は当面 OFF)
2. T3.2a を `track_bias_30d_waku_inside_outside_score` に rename
3. `_confidence` の閾値 (`min_score=110, min_gap=25`) の B1-S1 後 sensitivity test を plan v2.1 に追記

---

### 収益性判定家 (3.3 / 5, -0.2)

本改修は診断 script + scorecard のみで backtest 数値は更新されていない (最新 filtered backtest は `20260517_133637_tan_p17_A2_holdout-filtered.json` で **PROD 2026 控除率 52.1% / 絞り 65.7%**)。控除率 80% を超えるものは過去の TEST 絞り 88.7% のみで PROD では超えていないため、ルーブリック準用で **総合 3 以上の上限は 3.5**。

**3 問への回答**:
- **Q1 (H3-2 Welch p=0.81 の解釈)**: 「power 不足で判定不能」に 60-70% 重み。Apr-May 限定の structural shift が真因なら TRAIN を 2024 末まで rolling forward しても 2026 Apr-May 崩壊は再現しうる。**B1-S2 でのバックテスト評価は Q1 / Q2 別に分割必須**
- **Q2 (track_10/07 で wl_kelly_ge_05 停止すべきか)**: **部分停止**。`config.BUY_FILTER_DEFAULT` の `whitelist_tracks` から **track_07 / track_10 を即除外** + `min_starter_count=9` 試験追加 (= ゼロコスト防御)。track_05 (9.3%) / track_09 (13.2%) は改善方向で全場一律停止は機会損失
- **Q3 (B1-S1 4 features 改善見込み)**: 控除率 80% 超え確率 **0.30-0.45** (= plan v2 §1.2「holdout >= 80% 利益化 0.15-0.25」より上方修正、causal candidate を 4 features で **直接 cover** する設計のため)。中央値見込み: val Brier -0.004 / 2026 hold-out 絞り 76% / 採用 130 件 / Kelly 平均 0.012

**改善提案 3 件 (即時運用変更含む)**:
1. **即時運用変更**: `config.BUY_FILTER_DEFAULT` から track_07 / track_10 除外 + `min_starter_count=9` 試験追加
2. **Backtest 評価を Q1 / Q2 / Q3-4 で分割** — `scripts/backtest.py` に `--by-quarter` flag 追加、binary 4 条件を季節別評価
3. **Brier 警告自動サスペンド配線** — track 別月次 Brier が baseline +30% で当該 track のみ whitelist から自動除外を `scripts/monitor.py` に追加

---

### データパイプライン技術者 (4.2 / 5, -0.2)

担当範囲のコア (`jvlink_client/*`, `db.py`, `data/schema.sql`) は **10 連続改修ゼロ継続**。今回は SQLite を読み手として使う diagnostic + ingest 仕様の audit 文書化。**減点 -0.2** は「audit で構造的欠陥が可視化された」もので改修なしの単純な可視化結果:

- T3.3a audit FAIL で **WH record ingest_state 不在が明文化** (= ingest idempotency 4 → 3)
- `turf_condition='0'` 一律問題 + `starter_count >=19` garbage が H3-3 ログで定量化 (= スキーマ整合性 4 → 3)
- `q1_root_cause.py:67-83` の N+1 query (1776 picks × 1 SELECT)、idempotent だが scaling 不健全

**改善提案 3 件**:
1. **`turf_condition='0'` 一律問題の root cause 調査 + parser fix** — `parser.py:126` `_ascii(rec, 889, 1)` の byte offset を JV-Data RA record `TenkoBaba` 仕様と突き合わせ。fix できれば T2.2b `track_going_top3_rate` が B1-S1 復帰可能
2. **`schema.sql` に WH record ingest state 3 カラム追加** (`wh_ingest_data_div` / `wh_ingest_timestamp` / `wh_publish_timestamp`) — T3.3a 採用への前提解除、現在 9 連続持ち越し中の課題と一括で 1 ingest 改修セッション
3. **`scripts/q1_root_cause.py:67-84` の N+1 fix + race lookup util 化** — `WHERE (race_year, race_month_day, track_code, race_num) IN (VALUES ...)` の batch SELECT、`predictor/features.py` の T2.1b で再利用

**警告**: 前回 4.4 のピーク後 4.2 へ。「audit して放置」状態が積みあがると 3.5 へ滑落、次回までに改善提案 1 or 2 に 1 件着手で +0.3 回復見込み。

---

### **コード品質レビュアー (3.4 / 5, -1.1 ⚠ 後退)**

**最大の減点ポイント** (項目 3 マジックナンバー: 4.6 → 2.5, -2.1):
- `season_brier_test.py:27-40` の **MONTHLY_BRIER_2025/2026 dict が v4 §1.1 N1 表からの手書き引き写し**。`data/monthly_brier.log:3-19` (csv ブロック) に同じ 17 ヶ月分が機械可読 format で既に存在、`csv.reader` 5 行で読めるところを 14 行のハードコードで複製。「v4 表を 0.0001 単位で書き写すミス」がいつ起きてもおかしくない
- `compute_delta_sigma()` の `z >= 2.0` / `z >= 3.0` 判定 threshold は本文中 literal、Gate-2 (c) 判定の核 threshold なのに `--z-threshold` で CLI 化されていない

**DRY 違反** (項目 1: 4.7 → 3.0, -1.7):
- `scripts/q1_root_cause.py:87-157` の bucket function 6 種が **完全に同型の if-else 階段** (閾値配列 + ラベル配列 → return label)。factory 化で 70 行 → 25 行に圧縮可能
- `tm_rank` の `bucket_dm_rank` 流用 hack (= 「同じ bucket_fn 流用」コメント) が設計的にも factory 化が示唆

**改善提案 3 件**:
1. **`season_brier_test.py` の MONTHLY_BRIER dict 撤廃 → `data/monthly_brier.log` 自動 parse 化** — v4 表更新時の手書きミス防止が一番の価値、項目 3 が 2.5 → 4.0 に回復、総合 3.4 → 3.7
2. **`q1_root_cause.py` bucket function 6 種 → factory 化** — `BUCKETERS = {"odds": make_bucketer([(0, "missing"), ...]), ...}` の dict、項目 1 が 3.0 → 4.0、総合 3.4 → 3.6
3. **`docs/phase_b1_leak_audit.md` 9 section literal template → table 形式正規化** — 4 項目 × 9 features の audit Y/N は markdown table 1 枚で 11 行に圧縮、レビュー一覧性 +1 段

---

### **検証プロセス監査人 (3.8 / 5, -0.75 ⚠ 後退)**

**大幅減点 2 項目**:

**項目 3 (calibration / 統計検定設計): 4.4 → 3.0 (-1.4)**:
- primary test の power 計算事前公開なし: Welch n=5 vs 5 / delta ≈ 0.003 / pooled std ≈ 0.020 で β(中効果) ≈ 0.20-0.40 (検出力 60-80% 以下)
- 「FAIL = bias なし」と「FAIL = power 不足」が log/scorecard 上分離不能のまま **「公式 Gate-2 (a) 判定」確定**
- primary を Jan-May 集約に設定したのは handoff 由来だが、handoff 自体が Apr-May 限定の +3.3σ を観察源にしているため **検定母集団と効果源の不整合** (= conceptual mismatch)
- 事故 4 は「予想外し」でなく「primary が wrong test」設計問題

**項目 5 (過適合監視 / 多重検定): 4.4 → 3.2 (-1.2)**:
- H3-3 で 8 軸 × 平均 5 buckets/軸 ≈ 40 bucket の delta z-test を実施し |z|>=2.0 cutoff で 4 candidates 報告
- **Bonferroni 補正 α/40 = 0.00125 (≈ |z|>=3.0) を適用したら 0 candidates**、Benjamini-Hochberg FDR でも survive 不明
- `q1_root_cause.py` 内に multiple-comparison adjustment 関数なし、family-wise error rate (FWER) 監視欠落
- **Gate-2 (c) の「PASS (強)」判定はこの未補正で出した数字**

**改善提案 3 件**:
1. **最優先**: `scripts/season_brier_test.py` に **事前 power 計算と "FAIL の 2 義性" 明示出力**を追加。`statsmodels.stats.power.TTestIndPower` で β 計算、log の Gate-2 verdict に「primary FAIL: power=0.XX、effect size delta=0.003 では bias 「あり」も「なし」も distinguishable でない」を 1 行明記
2. **Bonferroni / BH-FDR 補正と family 数管理を q1_root_cause.py に実装** — `compute_delta_sigma` の出力に `n_tests_in_family` 追加、Gate-2 (c) PASS 判定を **Bonferroni-survivor 数 >= 1** に再定義する patch を plan v3 で提示
3. **Gate-2 三条件 binary 評価の "閾値設計" を plan v3 で開示・固定** — 「(a) FAIL かつ power<0.80 → (a) 保留扱い + replication test (= U12 permutation / Q1-vs-Q2 分割) を必須付帯条件に追加、replication PASS で `(a) provisional PASS` 昇格」を明文化。訂正 14 件 / 閾値 13 件超過に対する「rule freeze 解除 trigger」を CLAUDE.md ルール 1-quater 化候補として記録

---

## 横断的に見た優先課題

**⚠ 警告**: code-quality (-1.1) と validation-process (-0.75) の 2 名で大幅後退。両方とも本セッションで追加した診断 script 2 件の設計品質に起因しており、Gate-2 判定の信頼性そのものに影響する。

1. **【最優先】統計検定設計の根本欠陥を plan v3 で fix** (担当: validation-process-auditor + code-quality-reviewer の合流点)
   - 検証プロセス監査人の指摘: power 計算事前公開なし + primary 検定の effect 源整合性なし + 多重検定 Bonferroni 未補正
   - コード品質レビュアーの指摘: `z>=2.0` threshold が literal hard-coded で再現性 / 感度分析不能
   - 統合対応: `scripts/season_brier_test.py` に `--target-power 0.80` + `--effect-size-source primary` 引数追加、`scripts/q1_root_cause.py` に Bonferroni / BH-FDR 補正と `--family-size N` 引数追加。これらは plan v3 起こし review の前提となる
   - **本セッションの「Gate-2 (a) FAIL」「Gate-2 (c) PASS (強)」の verdict は両方とも統計設計欠陥の上に立っており、critic verdict 受領時にこれを正直に伝える必要**

2. **【高優先】実弾運用の即時防御 — track-specific サスペンド** (担当: profitability-judge)
   - `config.BUY_FILTER_DEFAULT.whitelist_tracks` から **track_07 / track_10 を即除外**
   - 工数 ~30 分、ゼロコスト防御、B1-S1 で T2.3a (track_recent_top3_rate_30d) が学習されるまでの bridge
   - H3-3 で hit_rate 2.8% / 4.2% = 控除率 80% を維持できない水準が data 上明確

3. **【中優先】docs/phase_b1_leak_audit.md の table 化 + B1-S1 4 features の weights namespace 設計 1h で先行着手** (担当: code-quality + prediction-logic の合流点)
   - leak audit は section 反復 (517 行) で summary table 不在、`MONTHLY_BRIER dict` と同じ「複製による正本不在」問題
   - B1-S1 で 4 features を実装する前に weights.json._comment_b1 に `track_prior` + `pace_estimate` 2 namespace の既定値 0 を先入れ、multicollinearity overfit 予防

---

## 集計平均が「健康に見える」が内訳は不健康な構造への注意

平均 3.86 → 3.87 (+0.01) で表面上同等だが、内訳は **2 名 -0.75 以上 + 3 名 -0.1 〜 -0.2 + 1 名 +0.2 + 1 名 ±0**。本セッション追加した診断 script の品質課題が予測 / 収益 / pipeline の本体結論に影響する場面で減点が連鎖。次回 (= plan v3 起こし review or B1-S1 着手) では:
- 統計検定設計の補正 (= 最優先 1) を完了させてから Gate-2 verdict を再評価
- code-quality と validation-process は両方とも improvement plan を 1 件以上着手して回復見込み +0.5 を狙う

**critic SendMessage で本 expert-review 結果を 1 行サマリで伝達**: 「expert-review 平均 3.87 (-0.0 vs 前回 3.86)、ただし内訳で code-quality -1.1 + validation-process -0.75 の大幅後退、両方とも本 B1-S0 で追加した診断 script の設計品質課題 (power 計算欠落 / 多重検定未補正 / DRY 違反 / マジックナンバー) に起因し Gate-2 判定の信頼性そのものに影響」
