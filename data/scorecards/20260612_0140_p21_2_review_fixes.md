# 採点 2026-06-12 01:40

**改修内容**: P21-2 (commit 14c12dc) — P21 expert-review 指摘の是正: 買い目フィルタ単一出典化の完遂 (BUY_FILTER_DEFAULT merge + JS フォールバック撤去 + null クリア + 注記動的注入) / キャッシュ世代カウンタ + 完了側 invalidate / btSeq ガード / 中止の venv64 subprocess 伝播 (Popen + kill, 0.5s) / n<30 表示 / placeholder viewport
**対象ファイル**: gui/app.py

## 総合スコア (項目平均) の推移

| 専門家 | 今回 | 前回 | 差分 |
|---|---|---|---|
| GUI / UX 監査人 | 4.5 | 4.2 | +0.3 |
| モバイル HTML レビュアー | 4.8 | 4.8 | ±0 |
| 予想ロジック分析官 | 4.5 | 4.4 | +0.1 |
| 収益性 / 投資判断 | 3.7 | 3.4 | +0.3 |
| データパイプライン技術者 | 4.2 | 4.0 | +0.2 |
| コード品質 / 保守性 | 4.1 | 3.9 | +0.2 |
| 検証プロセス監査人 | 3.9 | 3.8 | +0.1 |
| **平均** | **4.24** | **4.07** | **+0.17** |

後退項目なし。全 7 名 GATE: PASS。

## 各専門家の所見 (要点)

### GUI / UX 監査人 (4.5, +0.3)
- 進捗/キャンセル 5/5: 「最長ステージで中止が効かない」を Popen + kill で解消 (実測 0.5s)
- 二重実行防止 4.5: 世代カウンタ + btSeq + 完了時 invalidate で stale レース 3 種閉鎖
- 残: _error_hint に venv64 パターン無し (2 回持ち越し) / Python 側 running ミューテックス不在 (TOCTOU) / 予想生成中の経過秒表示

### モバイル HTML レビュアー (4.8, ±0)
- placeholder viewport 実装でエッジケース閉鎖。回帰ゼロ
- 注意: index.html が **1MB 突破** (1,004,723 bytes、データ蓄積由来) — サイズ監視の優先度上昇
- 繰越: buy-board summary 44px / placeholder color-scheme メタ

### 予想ロジック分析官 (4.5, +0.1)
- 本番運用との乖離 4→5: **GUI 買い候補 = backtest buy_only 検証集合が成立** (min_kelly/max_predicted_p 脱落の閉鎖)
- 残: dict(BUY_FILTER_DEFAULT) は shallow copy (リスト共有、現状読取専用で実害なし) / publish・run_prediction の過剰 invalidate / placeholder「なし」の将来ドリフト

### 収益性 / 投資判断 (3.7, +0.3)
- 買い目フィルタ 2.5→4.0: blocking 指摘 3 点を完全是正。**「実弾投資前に必須」とした修正は解消**
- 提案: override 時の「検証外フィルタ」バッジ / ダッシュボードに buy_only 行併記 / P15 賞味期限 2026-08-16 (残り 2 ヶ月) のカウントダウン表示

### データパイプライン技術者 (4.2, +0.2)
- 鮮度管理 4.5: max_odds_age_min 単一出典化 + 世代カウンタの lock 規律が正確
- 指摘: **完了側 invalidate が失敗系 (ingest 途中の例外/中止) で未発火** → 本 scorecard 保存と同コミットで `_safe` の例外/中止ハンドラに invalidate を追加して閉鎖済み (スモーク検証済)
- 繰越: DB mtime のキャッシュキー混入 (GUI 外部書込み検知)

### コード品質 / 保守性 (4.1, +0.2)
- DRY 4.5: FILTER_INPUTS マップ / merge / 動的注記の 3 層で単一出典化完遂
- 提案: bet_filter merge の pure 関数化 + 回帰テスト / 上書き可能キー集合の Python↔JS 平行記述解消 / refreshBacktest の catch 可視化

### 検証プロセス監査人 (3.9, +0.1)
- **3 回持ち越しの n<30 ガード完遂** (降格宣言を回避)
- 指摘: 完了側 invalidate の例外パス (→ 同上、閉鎖済み) / **anchor 鮮度警告は 4 回目の持ち越し** (次回未着手で calibration 項降格) / n<30 閾値の Python↔JS 二重記述

## 横断的に見た優先課題 (次回改修向け)

1. **anchor 鮮度警告** (validation 4 回持ち越し、降格宣言あり) — `_recent_backtest` に anchor_age_days を追加し 7 日超で警告表示
2. **bet_filter merge の pure 関数化 + 回帰テスト** (code-quality) — 今回潰した「min_kelly 脱落」と同型の回帰を tests/ で恒久ブロック。上書き可能キー集合の Python↔JS 単一出典化も同梱
3. **override 時の「検証外フィルタ」バッジ + buy_only 行併記** (profitability) — GUI フィルタを触った瞬間に backtest 93.1% の検証保証が消えることの可視化

## 追補 (scorecard 保存と同コミット)

- data-pipeline + validation 両名の最優先指摘「完了側 invalidate の失敗系未発火」を `_safe` の except/CancelledError ハンドラへの `_invalidate_caches()` 追加で閉鎖。エラー +1 gen / 中止 +1 gen をスモークで確認、pytest 12 passed。
