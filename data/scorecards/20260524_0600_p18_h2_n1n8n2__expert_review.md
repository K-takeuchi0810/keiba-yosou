# 採点 2026-05-24 06:00 — P18 H2 (N1+N8+N2+N9, v4 scorecard, SOME-PASS)

**改修内容**: P18 H2 検証 (N1 月次 LGBM Brier + N8 fold 分割 sensitivity + N2 同オッズ帯 LGBM vs MING + N9 R% 1pp 刻み)。`scripts/` に 5 本新規追加、`data/scorecards/20260524_0530_p18_h2_results_v4.md` で Phase B1 着手判定 **SOME-PASS (2/3)**。本体ロジック (`gui/` `predictor/` `jvlink_client/` `web/`) は **一切未触**。

**対象ファイル**:
- `scripts/dump_predictions.py` (新規 202 行、predict_race 経由 dump、130 min wall で 4776 picks)
- `scripts/monthly_brier_analyze.py` (新規 160 行、N1)
- `scripts/fold_sensitivity_analyze.py` (新規 262 行、N8)
- `scripts/odds_tier_analyze.py` (新規 178 行、N2)
- `scripts/cliff_sensitivity_fine.py` (新規 104 行、N9)
- `data/scorecards/20260524_0530_p18_h2_results_v4.md` (中核成果物)
- `data/h2_session_notes.txt` (3 欄構造 + 事前固定 rubric)

---

## 7 名総合スコア

| Subagent | 今回 | 前回 (20260518) | Δ |
|---|---:|---:|---:|
| gui-ux-auditor | 3.8 | 3.8 | ±0 |
| mobile-html-reviewer | 4.6 | 4.6 | ±0 |
| prediction-logic-analyst | 4.15 | 4.35 | -0.20 |
| **profitability-judge** | **2.0** | 3.5 | **⚠ 後退 -1.5** |
| data-pipeline-engineer | 4.3 | 4.4 | -0.1 |
| **code-quality-reviewer** | **2.8** | 4.5 | **⚠ 後退 -1.7** |
| validation-process-auditor | 4.6 | 4.55 | +0.05 |
| **平均** | **3.75** | **4.24** | **-0.49** |

**警告**: 2 名が -0.3 以上後退 (profitability-judge -1.5、code-quality-reviewer -1.7)。

---

## 各専門家の所見

### GUI / UX 監査人 — 3.8 (±0)

GUI 関連ファイルは P17 S7-γ 以降一切変更なし。本セッションは scripts/ と data/ のみで GUI/HTML 完全に手付かず。前回スコアを変動させる事由なし。

CONTROL_HTML の `min_kelly` / `max_predicted_p` 入力欄欠落 (`gui/app.py:1438-1441`) は **4 期連続温存** が最優先課題。Phase B1 着手前に処理しないと、運用フィルタを GUI から直接いじれない状態が続く。

### モバイル HTML レビュアー — 4.6 (±0)

`web/` 配下に S7 以降のコミット一切なし。HTML 描画経路への影響なし。タップ領域 / 情報密度 / ダークモード / iOS 互換すべて 5 点維持。

H2 結果から派生する HTML 改修候補: 「採用戦略の賞味期限残量」「直近 1Q rolling Brier」を `version-snapshot` に表示する案。Phase B1 で `weights.json` が更新されたタイミングで同期検討。

### 予想ロジック分析官 — 4.15 (-0.20)

本体 (`predictor/rules.py` / `features.py` / `weights.json`) 未触だが、H2 が暴露した 4 観察 (高 p 帯 over-confidence 残存、Apr-May drift、2026Q1 単独崩壊、cliff at R=76) で評価軸が更新された。

主な後退:
- **信頼度判定 / 確率推定: 4 → 3.5 (-0.5)**: H2 N1 reliability で bin [0.2-0.5] gap -15〜-42pp が定量化、`predictor/rules.py:806-809` の race 内 Σ=1 再正規化と Isotonic 校正の二重正規化問題が顕在化
- **デッドコード / 整合性: 4.75 → 4.5 (-0.25)**: `predictor/calibrator.json:80` の `"calibrator_type": "bin"` と `"calibrator_rule_version": "p07-train-21-23"` が top-level `"type": "isotonic"` (l.2) と矛盾

最優先改修候補: `predictor/calibrator.json` の metadata 矛盾 (1 行 patch) + `config.BUY_FILTER_DEFAULT.max_predicted_p` を 0.40 → 0.30 に一段締める。

### 収益性監査人 — 2.0 (⚠ 後退 -1.5)

**赤信号 3 件**:

1. **config 認識ズレ**: 私 (= 親 Claude) が profitability-judge に渡したプロンプトで「現状 BUY_FILTER_DEFAULT = whitelist_tracks ["07","09"] + min_odds 8.0/max_odds 20.0」と書いたが、これは `.claude/skills/project-state/SKILL.md` (2026-05-12 snapshot) の **古い記述**。S5-3 (2026-05-17 P17) で既に `min_kelly=0.05 + max_predicted_p=0.40 + 全場開放` に切替済。**project-state.md と実 config.py の乖離**を agent が指摘
2. **SOME-PASS は買い目即時改善案を含まない**: v4 §5 推奨アクション 1-4 は Phase B1 plan への入力で、現 config の実弾運用 (P17 holdout buy_only 65.7%) は控除率以下のまま継続
3. **阪神 (09) 単独 137% を全場開放で薄めている**: P17 holdout で阪神単独 216 bets / 137% (CI [55%, 204%])、全場混入 1380 bets / 52%。S5-3 で全場開放した「機会が薄くなる」根拠は H2 quarterly drift 後の今は再検証必要

**即時推奨**:
- `whitelist_mode=True + whitelist_tracks=["09"]` への暫定切替を Phase B1 完了まで採用
- bet sizing circuit breaker (月次 drawdown -15% で bet_unit 半減、-30% でサスペンド)
- `scripts/cliff_sensitivity_fine.py` を `weekly_monitor.bat` に統合

### データパイプライン技術者 — 4.3 (-0.1)

**主な後退**:
- **データ鮮度管理: 5 → 3 (-2)**: 4776 races dump で `races` 2026-05-17 まで vs `payouts` 2026-05-10 までの 7 日ラグを露呈。`fetch_state.json` の race vs payout 別タイムスタンプが見えない構造的問題
- 4776 races / 130 min long-run で WAL + read transaction + feature_cache が問題なく稼働 → リトライ/パフォーマンス +1

最優先改修: `get_payout` を 3 値 (1/0/None=未取込) に拡張し、`y_win` を 3 値化。`monthly_brier_analyze` 等は unknown を skip。H3-1 (MING fold shift が ingest coverage 由来か挙動由来か) の切り分けが可能になる。

### コード品質レビュー — 2.8 (⚠ 後退 -1.7)

**最大の後退の根拠**:

1. **DRY 違反**: 5 本間で `aggregate` 関数、fold 期間定義 (5 箇所重複)、`classify` 関数が重複コピペ
2. **cutoff date 不整合**: `oracle_diagnose.py` (20260517) vs `odds_tier_analyze.py` (20260510) で 2026P fold cutoff が 7 日違い。**N1 と N8 で picks 数が物理的に違う可能性**
3. **filter_sweep.FILTERS との重複**: `fold_sensitivity_analyze.STRATEGIES` は `kelly_ge_05 / ev_ge_105 / dm_rank_1_3` を lambda で再宣言、既に `scripts/filter_sweep.py:157` の `FILTERS` で名前付き管理されているのを使っていない
4. **マジックナンバー散在**: robust 閾値 (0.80, 0.50, 10, 1000, 30, 5, 15) を hardcode で散在、`h2_session_notes.txt 通り` というコメントしか根拠なし
5. **dead code / 未使用 import**: `odds_tier_analyze.py:19` の `import statistics` 未使用、`fold_sensitivity_analyze.py:61-68` の `scheme == "reverse"` 分岐が "current" と完全同一コードで sanity check になっていない
6. **try/except 0 件**: 5 本全部で例外ハンドリングなし、`StopIteration` 危険箇所 (`cliff_sensitivity_fine.py:68`)

短時間で 5 本量産する検証セッションという性質上、共通化を先送りした結果、`filter_sweep.py` / `oracle_diagnose.py` 既存資産との重複が積み上がった。**`scripts/_h2_common.py` を切り出すまで Phase A1+A2 baseline (4.5) に戻すことは不可能**。

### 検証プロセス監査人 — 4.6 (+0.05)

**本セッションの最大成果**:
- §0.0 dataset 上限 + controlled-ness 軸の導入で「単一観察での確定言明」(v3 §4 t5 系) を構造的に予防
- §5 ALL/SOME/NO-PASS rubric で v3 「weakly clear」連続量表現を binary 合成に置換
- N8 で 4 種 fold scheme を「事前固定」してから走らせ、「2026P 単独崩壊」narrative が H1/H2/2026P binning artifact と判明させた手順は範例的
- §4 事故 1 (N9 scope creep) を「発生事実 + 次回予防」prospective 警告フォーマットで記録

軸別:
- 過適合監視: 4.4 → **4.9** (+0.5) **大幅改善**
- calibration 計測: 4.4 → 4.6 (+0.2) 改善
- バックテスト設計: 4.7 → 4.5 (-0.2)
- A/B 比較 / バージョン管理: 4.7 → 4.4 (-0.3) 後退: 前回宿題 (`backtest_diff.py`) 未着手、dump CSV に meta 同梱なし

最優先改修: `dump_predictions.py` に `scripts/backtest.py:34-75` の `_snapshot_meta()` を移植 (CSV 1 行目コメント or sidecar JSON で calibrator/LGBM rule_version + git_sha 等)。

---

## 横断的に見た優先課題 (3 件)

### ⚠ 警告: 2 名で -0.3 以上後退

平均 4.24 → 3.75 (-0.49) は **過去最大の後退**。原因は (a) H2 が「scripts/ に 5 本量産する検証セッション」性質で共通化を先送り、(b) H2 結果が H1+H2 の検証成果と引き換えに predictor / config 側の負債を顕在化させたこと。

### 優先 1: `scripts/_h2_common.py` への共通化 + cutoff date 統一 (担当: code-quality-reviewer)

**内容**:
- `FOLD_PERIODS` (date 含む、cutoff 統一)、`STRATEGIES` (lambda ベース)、`aggregate(rows, strat_filter=None)`、`classify_robust(bets, rets, los, R, L, min_bets)` を 1 ファイルに切り出し、5 本すべてが import
- `2026P` fold cutoff の `20260510` vs `20260517` 食い違いを物理的に再発不能にする (= N1 / N8 / N9 で picks 数が違う可能性を排除)
- robust 閾値 (R/L/min_bets/n_resample) を `data/h2_config.json` に外出し
- `fold_sensitivity_analyze.STRATEGIES` を `filter_sweep.FILTERS` 駆動に変更

**工数**: 1-2h
**影響**: code-quality 2.8 → 4.0 程度、技術的負債解消

### 優先 2: profitability 即時対策 (担当: profitability-judge + validation-process-auditor)

**内容**:
- `whitelist_mode=True + whitelist_tracks=["09"]` への暫定切替を Phase B1 完了まで採用検討 (阪神単独 137% を全場開放で薄めている現状の改善)
- bet sizing circuit breaker: 月次 drawdown -15% で `bet_unit` 半減、-30% でサスペンド
- `scripts/cliff_sensitivity_fine.py` を `weekly_monitor.bat` に統合 (R 76-80 Y 件数監視)
- `.claude/skills/project-state/SKILL.md` の `BUY_FILTER_DEFAULT` 記述を S5-3 (2026-05-17) 状態に更新 (= 古い snapshot で agent を contaminate した事故の構造的修正)

**工数**: 30-60 分
**影響**: profitability 2.0 → 2.5-3.0、実弾運用面の安全策確立

### 優先 3: `dump_predictions.py` への `_snapshot_meta` 移植 + `predictor/calibrator.json` metadata 矛盾解消 (担当: validation-process-auditor + prediction-logic-analyst)

**内容**:
- `scripts/dump_predictions.py` に `scripts/backtest.py:34-75` の `_snapshot_meta()` を移植 (sidecar JSON `data/dump_picks_h2.meta.json` で calibrator/LGBM rule_version + git_sha + n_picks + from-to を記録)
- `predictor/calibrator.json` の metadata 矛盾解消: `"calibrator_type": "bin"` → `"isotonic"`、`"calibrator_rule_version": "p07-train-21-23"` → 適切な rule version

**工数**: 30 分
**影響**: 6 ヶ月後の追跡可能性、検証プロセス +0.2、予想ロジック整合性回復

---

## 副次発見 (横断的)

- **MING dm_rank_1_3 の odds median fold shift (40.4 → 7.7)** が data-pipeline 由来か MING ロジック変更由来かの切り分けは H3-1 で実施予定
- **race 内 Σ=1 再正規化 (rules.py:806-809) と Isotonic 校正の二重正規化** が H2 N1 reliability で再確認、Phase B1 LGBM v6 訓練の loss 設計 (listwise/softmax) と同期して再設計推奨
- **payouts 追従ラグ 7 日 (2026-05-10 ↔ 05-17)** が `monthly_brier_analyze` で 2026-05 picks 落ちを生む構造リスク、H3-1 と並行で `get_payout` 3 値化推奨

---

## 個別 agent ID (継続会話用)

- gui-ux-auditor: aa735cb22e354e89e
- mobile-html-reviewer: a97189d8c0fd74f97
- prediction-logic-analyst: a74f89a7bc6df56cf
- profitability-judge: a93c5eb8f2340cb7b
- data-pipeline-engineer: afbb725bd13c84890
- code-quality-reviewer: a28d66326e83dccf2
- validation-process-auditor: a311029561e95e10c

---

## 関連 scorecard

- 親 scorecard: `data/scorecards/20260524_0530_p18_h2_results_v4.md` (H2 結果本体)
- 前回集約: `data/scorecards/20260518_phase_a1_to_s7_summary.md` (平均 4.24)
- handoff: `data/scorecards/20260523_2230_p18_handoff_to_next_session.md`