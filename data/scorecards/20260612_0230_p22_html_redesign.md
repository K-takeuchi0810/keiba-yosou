# 採点 2026-06-12 02:30

**改修内容**: P22 (commit 0b91f6a) — ユーザフィードバック対応: プレビュー「✕閉じる」+ Esc / 「自動」→「最新開催」改名 + 解決日付の書き戻し / 予想 HTML デザイン改修 (本命プレビュー行・買い候補カード 2 ブロック化・開催日ナビチップ・日別買い候補数・印行ハイライト・繰越 2 件解消)
**対象ファイル**: gui/app.py, web/templates/index.html.j2
**追補**: P22-2 (commit 9fa0388) でレビュー必修指摘を即時是正 (下記)

## 総合スコア (項目平均) の推移

| 専門家 | 今回 | 前回 | 差分 |
|---|---|---|---|
| GUI / UX 監査人 | 4.5 | 4.5 | ±0 |
| モバイル HTML レビュアー | 4.6 | 4.8 | -0.2 |
| 予想ロジック分析官 | 4.5 | 4.5 | ±0 |
| 収益性 / 投資判断 | 3.8 | 3.7 | +0.1 |
| データパイプライン技術者 | 4.2 | 4.2 | ±0 |
| コード品質 / 保守性 | 4.0 | 4.1 | -0.1 |
| 検証プロセス監査人 | 3.8 | 3.9 | -0.1 |
| **平均** | **4.20** | **4.24** | **-0.04** |

-0.3 超の後退なし。全 7 名 GATE: PASS。

## 各専門家の所見 (要点)

### GUI / UX 監査人 (4.5, ±0)
- ✕閉じるタブ (preview 中のみ表示 + Esc) で迷子導線を閉鎖。「最新開催」改名 + 書き戻しで「空欄=何が出てるの?」問題を根治
- 指摘: _error_hint に venv64 パターン無し 3 回目 (次回降格宣言) → **P22-2 で解消**
- 残: Python 側 running ミューテックス (TOCTOU) / 予想生成中の経過秒表示

### モバイル HTML レビュアー (4.6, -0.2)
- 情報密度 5/5: head-pick で「開かずスキャン」成立、「項目内では今回が過去最良」。2 回持ち越し (buy-board summary 44px / --bg-mute) 完遂
- 新規指摘 3 件: ①アンカー遷移先が sticky ヘッダに潜る (scroll-margin-top 欠落、次回必修) ②day-nav チップ高 27px ③dark の day-buy-count ~2:1 AA fail → **3 件とも P22-2 で解消** (実測クリアランス +9px / チップ高 35px / 暗色チップ化)
- 旗: index.html 1.1MB (3 回目、データ蓄積由来) — サイズ監視は未着手

### 予想ロジック分析官 (4.5, ±0)
- 表示専用改修で計算経路への波及ゼロを契約 (テンプレ参照 ⇔ generator 供給) の両面で確認。head-pick の「暫定」併載は誤読防止に正
- 提案: head-pick にモデル確率 P 併記 / P15 賞味期限 2026-08-16 (残り 2 ヶ月)

### 収益性 / 投資判断 (3.8, +0.1)
- Kelly 表示 3.5→4.0: 推奨投資率の視覚分離は「full Kelly への anchoring」を実害レベルで低減
- 新規指摘: **over-cap 日の buy-reco-num が縮小前の値のまま** (portfolio-note の ⚠各点×scale と不整合) — 次回改修候補の筆頭
- 提案: バンクロール額入力 → 円換算表示 / 根拠鮮度カウントダウン
- 「表示磨きでの上積み余地はほぼ使い切った。次に収益スコアを動かすのは数値側」

### データパイプライン技術者 (4.2, ±0)
- パイプライン非破壊を確認。「最新開催」の書き戻しは sticky → one-shot への意味論変更 (意図的 UX、把握事項)
- 繰越 2 回目: DB mtime のキャッシュキー混入

### コード品質 / 保守性 (4.0, -0.1)
- 指摘: day_buys の selectattr 重複 / --bg-mute フォールバック二重記述 / render smoke test 不在 → **3 件とも P22-2 で解消** (generator 側 buy_count 単一出典化 + tests/test_template_render.py 新設・StrictUndefined)
- 残: ymdToInput ↔ _normalize_date の暗黙契約コメント

### 検証プロセス監査人 (3.8, -0.1)
- **anchor 鮮度警告 5 回目の持ち越しに対し宣言済み降格を執行** (calibration 4→3)。「表示層コミットを挟むだけで検証負債を無期限延期できる抜け道を許さない」
- → **P22-2 で解消**: anchor_age_days / anchor_stale (>7日) + 「確定データN日前」表示 + n<30 閾値の Python↔JS 単一出典化
- 提案残: calibration.brier のダッシュボード露出

## 横断的に見た優先課題 (次回改修向け)

1. **over-cap 日の buy-reco-num 縮小後値表示** (profitability) — P22 で最も目立つ数字にした推奨投資率が、上限超過日には縮小前の過大値のまま。generator で日別 scale を各候補に付与して差し替え
2. **収益スコアの主戦場は数値側へ** (profitability) — backtest 根拠更新 / calibrator 再 fit (2025 通年 fit の鮮度劣化) / バンクロール円換算。表示改修での上積みは尽きた
3. **DB mtime のキャッシュキー混入** (pipeline 繰越 2 回目) — GUI 外部の DB 書込み検知

## P22-2 追補 (commit 9fa0388、scorecard 保存と同セッション)

レビューの「次回必修/降格」指摘を即時是正:
- scroll-margin-top 11rem (実測クリアランス +9px@375px) / day-nav チップ高 35px / day-buy-count 暗色化
- anchor 鮮度警告 (5 回持ち越し解消) + n<30 単一出典化
- _error_hint venv64 パターン (3 回持ち越し解消)
- tests/test_template_render.py 新設 (StrictUndefined render smoke、16 passed)
- day.buy_count 単一出典化 / --bg-mute フォールバック削除
