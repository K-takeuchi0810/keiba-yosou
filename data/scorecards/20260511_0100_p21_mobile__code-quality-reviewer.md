# コード品質 / 保守性レビュアー 採点

## 総合: 4.1 / 5  (前回 4.0 → **+0.1**)

CSS 変数化の意義: rules.py の `_w()` 集約と同じ「**マジック値の単一定義点化**」が CSS 層にも波及した。ダークモード対応箇所が `:root` 内 6 行の上書きで済む構造になり、配色変更の影響範囲が template 全体 → `:root` 1 ブロックに局所化。

## 項目別

- **DRY / 重複コード: 4.5/5** (前回 4.5 → ±0) — 影響なし。新規 6 変数 (`--buy-board-bg/-fg/-card-border/--tag-bg/-fg/--top-picks-bg`) は全て参照側で `var()` 経由 (`:105, 131, 215, 226, 234`) に統一済。同色直書きの再持込は grep 上ゼロ。

- **dead code / 未使用シンボル: 4/5** (前回 4 → ±0) — Python 側の残 dead feat 2 件は本改修対象外。新規 CSS 変数 6 件は全て参照確認済 (未使用変数なし)。`scripts/probe_*.py` 4 本も未対応。

- **マジックナンバー / 設定外出し: 4.9/5** (前回 4.8 → **+0.1**) — **CSS 層も `_w()` 化**。`#fff8f7 / #c0392b 25% / #eef2f7 / #444 / rgba(26,95,180,.06)` 等の直書き 10+ 箇所が `:root` 1 ブロックに集約。残る `#fff`/`#000`/`#888` は (a) 8 枠番カラー (`waku-1〜8`、JRA 規約で固定値、変数化する意義なし) と (b) 色付き背景上の白文字 (静的コントラスト保証で動的切替不要) のみ。`:24, 40-41` に「なぜ 6 変数を切り出したか / なぜダークでは pinkish を捨てたか」のコメントが入っており半年後も読める。`jvlink_client/client.py` retry 秒数が未対応で 5 に届かず。

- **テスト容易性 / 副作用分離: 2.5/5** (前回 2.5 → ±0) — 不変。CSS 側にテスト ROI は薄いが、変数経由になったことで「ダークモード時の色だけ snapshot 比較」が可能になった (将来余地)。

- **エラー処理 / ログ / 観測可能性: 4.5/5** (前回 4.5 → ±0) — 影響なし。CSS 変数欠損はブラウザが silent fallback、Python 例外パスに変化なし。

## 主な改善提案 (優先 1 件、最大 3 件)

1. **`waku-1〜8` を `--waku-1〜8` に変数化** — 現状 8 枠番カラー (`:249-256`) のみ唯一の直書き残骸。JRA 規約色なので可変性は低いが、将来「色覚多様性配慮モード」を入れる場合に `:root` 上書き 1 ブロックで済む。項目 3: 4.9 → 5.0、総合 4.1 → 4.2。
2. **`tests/test_weights.py` 新設** (前回 #2 を継続) — `_w()` 網羅率と weights.json 同期破れの CI 検出。項目 4: 2.5 → 3.5、総合 4.1 → 4.3。
3. **残 dead feat 2 件削除** (前回 #3 を継続) — `estimated_leg_samples` / `same_track_type_runs`。項目 2: 4 → 4.5。

## 前回からの差分

- 項目1 (DRY): 4.5 → 4.5 (±0) **維持**: 同色直書き再持込ゼロ
- 項目2 (dead code): 4 → 4 (±0) **維持**: 新規 6 変数は全て参照あり、Python 側未対応
- 項目3 (magic number): 4.8 → 4.9 (**+0.1**) **小幅改善**: CSS 層で 10+ 箇所外出し、ダークモード上書きが 1 ブロックに集約
- 項目4 (test): 2.5 → 2.5 (±0) **維持**
- 項目5 (logging): 4.5 → 4.5 (±0) **維持**

総合: 4.0 → **4.1** (+0.1) — スコープが CSS 層限定のため小幅だが、「マジック値の単一定義点化」原則が Python と CSS で揃った点は構造的価値が大きい。次レバーは waku 変数化 (#1) > tests/ 新設 (#2)。
