# 採点 2026-07-03 10:58

**改修内容**: LGBM v6 本番入替パッケージ (branch feature/lgbm-v6-relative-draw)。F1 相対化+F2 枠順 (98→112特徴、寄与ゼロと ablation 確定) / leg_code リーク除去 / cache キー衝突2件修正 / odds 信頼メタデータ事故の検出・修復 (261,163行) / LGBM v6 (val_brier 0.0615) / RULES_VERSION p26 / calibrator 再fit (2025, 47,884 records) / OOS walk-forward ハーネス + paired backtest。
**対象ファイル**: predictor/features.py, ml_model.py, rules.py, lgbm_* 成果物, calibrator.json, scripts/train_lgbm.py, eval_lgbm_oos.py, db.py, jvlink_client/ingest.py, config.py, tests×6

## 総合スコア (項目平均) の推移

| 専門家 | 今回 | 前回 | 差分 | 判定 |
|---|---|---|---|---|
| GUI / UX 監査人 | 3.6 | 3.6 | ±0 | PASS (無変更・JS パース OK) |
| モバイル HTML レビュアー | 4.6 | 4.6 | ±0 | PASS (無変更) |
| 予想ロジック分析官 | 4.0 | 4.0 | ±0 | PASS (条件付き) |
| 収益性ジャッジ | 3.2 | 4.0 | **⚠ -0.8** | **HOLD** (エッジなし継続=想定通り、実弾禁止維持) |
| データ基盤エンジニア | 3.8 | 4.4 | **⚠ -0.6** | **HOLD** (odds メタ事故の残渣 1 件未クローズ) |
| コード品質レビュアー | 4.0 | 4.2 | -0.2 | PASS |
| 検証プロセス監査人 (最終ゲート) | 4.3 | 4.3 | ±0 | **CONDITIONAL PASS → 統合 PASS** |

**統合判定: マージ可**。最終ゲートの統合条件 =「兄弟 6 名に FAIL/NOT_EVALUABLE が無いこと」→ 5 PASS + 2 HOLD / FAIL ゼロで充足。
両 HOLD とも「v6 入替自体は妥当 (観察専用として)」と明記しており、入替をブロックしない。
⚠ 2 件の後退はどちらも「新事実の発見による正当な下方修正」(収益性: フィルタ価値破壊の再確認 / データ基盤: 前回の自己過大評価の是正 + 事故残渣)。

## 中核数値 (第三者再現可)

### OOS walk-forward ハーネス (2024-2025, 6,909 races, scripts/eval_lgbm_oos.py — 最終ゲートが独立再実行で一致確認)
| モデル | AUC | top1 | flatROI |
|---|---|---|---|
| production-v5 (live条件=leg_code unknown) | **0.579** | 8.8% | 64.3% |
| v6 (本パッケージ) | **0.803** | 28.7% | 76.1% |

### paired backtest (同一修復後DB・20260101-0614・1,568 bets)
| | v6+p26 | v5+p21 |
|---|---|---|
| all 回収率 | 63.5% [56.5,70.9] | 68.9% [61.2,76.6] (leg_code リーク利得込み) |
| Brier / logloss | **0.0602 / 0.2213** | 0.0632 / 0.2388 |
| buy_only (pop1-3) | 56.3% (123) | 54.1% (194) |

- 入替論拠: backtest は歴史 DB の leg_code (post-race) を v5 に与える構造バイアスがあり、live 実力はハーネスが正 (最終ゲートが features.py:988/1197 の needs_post_race_data で裏付け、反証失敗=論証成立)。v6 は leg_code 不使用で backtest≈live。
- 確率品質は同一窓で v6 が明確に改善 (Brier -4.8% / logloss -7.3%)。
- **回収率のエッジは無い** (CI 上限<100%)。実弾禁止・観察専用継続。エッジは F3 の仕事。

## 4 バグの発見と是正 (本パッケージの実質的価値)

| # | バグ | 是正 | 回帰テスト |
|---|---|---|---|
| 1 | leg_code = post-race 値 (未走0%/確定100%) を v5 が特徴使用 → live でほぼランダム (AUC 0.579) | 特徴から除去 (721e95f) + 再流入ガード assert | ✅ |
| 2 | relative_race_metrics cache キーに馬識別なし (走査順で dataset が変わる) | キーに horse_num (fe55779) | ✅ |
| 3 | bloodline going cache キーに馬場なし | キーに going (fe55779) | ✅ |
| 4 | RACE バックフィルが確定オッズ 261,163 行に発走後 mtime 刻印 → 全レース odds ゲート除外 | historical モード (17fd8ab) + DB 修復 + 監査ログ docs/20260703_odds_metadata_repair.md | ✅ |

## 各専門家の所見 (要約)

### 検証プロセス監査人 (4.3, CONDITIONAL PASS→統合PASS) ★最終ゲート
ハーネスを独立再実行し数値一致。「v6 の他特徴が代わりにリークしていないか」の反証を ablation で試み失敗 (=真の利得は leg_code 除去で over-claim なし)。calibrator fit 窓分離・race 単位 bootstrap・walk-forward 順方向分離すべて合格。git_dirty=True は良性 (status_short が捕捉)。**残条件**: (1) 兄弟判定に FAIL なし→充足、(2) 本 scorecard 記録→本書、(3) マージ後 weekly_monitor baseline を p26 で freeze し直し。

### 収益性ジャッジ (3.2, HOLD)
paired 成立・証跡完備。ただし v6 も CI 上限<100% でエッジなし (想定通り) →「収益性が改善した」根拠はゼロ、観察専用として入替は妥当。**pop1-3 フィルタは両モデルで価値破壊継続 → 「観察マーカー」に格下げ明記** [対応済 config.py]。"clean-oos" ラベルは 06-28 baseline (1,135 bets) と footing が異なる点に注意 (5-6月が確定オッズ化で母集団入り。paired 内比較は無傷)。live 優位の収益的決着は CLV 実測が本筋 (F3)。

### データ基盤エンジニア (3.8, HOLD ⚠-0.6)
事故検出・historical モード・修復・回帰テストは実務水準。**自己監査**: 前回「冪等 5」はメタデータ列の write-path を見ておらず過大評価 → 「行数不変」でなく「信頼メタ列 write-path + ゲート通過レース数の不変」を冪等指標にする [監査ログに記録済]。**未クローズ**: 2026 5-6月の 9,027 行が確定値+NULL となり PIT タイムスタンプ復元不能 — F3 で発走前オッズ特徴を作る際に「NULL=発走後」規律のコード強制が必須 (validation 降格宣言(2) と同一)。修復の追跡子消失 → 監査ログで対応 [済]。

### 予想ロジック分析官 (4.0, PASS 条件付き)
train-serve skew 3 経路 (F1 両経路同一適用 / leg_code / cache) の解消を実コード確認。calibrator は expected=p26 完全一致で「refit 前の確率改善主張」に非該当。**mining 依存 67%** (46% は過小集計) = JRA-VAN 単一ソース依存 → weekly_monitor に mining カバレッジ監視を追加推奨。model_blend 重みは confidence 分布が v5≈v6 で流用は防御可能だが、v6 の品質向上分 model_weight 引上げ余地 (sweep 推奨、非ブロッキング)。

### コード品質レビュアー (4.0, PASS)
add_race_relative_inplace 単一関数を両経路共有=単一出典。4 バグ全てに回帰テスト。**変更失敗モード**: leg_code 再流入をコメントだけで防いでいた → ガード assert 追加 [対応済]。train/serve パリティテスト欠如 [対応済]。raw_leg_code の dead value [用途注記で対応済]。

### GUI/UX (3.6) / モバイル (4.6) — PASS
gui/web 無変更を実測、JS パース OK。HTML フッタの lgbm_rule_version は generator.py:520 が lgbm_meta.json から動的取得 → 入替後は自動で v6 表示 (stale 表示の罠なし、親レビューで確認済)。

## 横断的に見た優先課題 (マージ後・優先順)

1. **weekly_monitor baseline の p26 再凍結** (最終ゲート残条件3) — `python -m scripts.monitor --freeze-baseline-days 30`。旧 v5 baseline のままだと監視が空振る。CLAUDE.md ルール4 の月次監視は入替後も継続。
2. **F3 着手時の PIT ゲートのコード強制** (validation 降格宣言(2) + data-pipeline #1) — NULL fetched_at = 発走後確定オッズを発走前特徴に使わせないガードを、参照実装と同時に入れる。怠れば即 FAIL。
3. **mining カバレッジ監視 + model_blend sweep** (prediction-logic) — 単一ソース依存 67% の劣化検知、v6 品質向上を活かす model_weight 再調整。
