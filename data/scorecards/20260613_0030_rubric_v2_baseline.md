# 採点 2026-06-13 00:30 — rubric v2 再ベースライン

**改修内容**: コード改修なし。専門家 7 名 + ルーブリックを最上級水準 (v2) に改訂 (commit ef0ee00) した上での現状再監査。
**v2 の要点**: 外部絶対基準 (一流実務家が本番承認できるか) / 証拠規律 (実測・反証・事実推論区別) / model: fable 固定。
**重要**: 本スコアは再ベースライン。v1 スコアとの差は「後退」ではなく基準の錨替え。CLAUDE.md の -0.3 警告は今後の v2 同士の比較に適用する。

## 総合スコア (v2 ベースライン)

| 専門家 | v2 | v1 参考値 | v2 で新たに検出した主要問題 |
|---|---|---|---|
| GUI / UX 監査人 | 3.6 | 4.5 | --text-mute コントラスト 3.17-3.73:1 AA fail (実測) / 予想生成ステージの進捗ゼロ / ingest 中はキャンセル不達 / aria-live 不在 |
| モバイル HTML | 3.6 | 4.6 | **dark の買い候補ボード 白/#ff8a7a = 2.29:1** (最重要ブロックが系統的 AA fail、1 行で修正可) / 「0.0倍」露出 3 件 / 1.05MB (予算超過 3 回目) |
| 予想ロジック | 3.2 | 4.5 | **sprint_multiplier が n=25 根拠の過適合現存例** / 重み 0 シグナルが根拠文に出る虚偽表示 / dead weights 2 key / env 上書きが校正器を無言で無効化 |
| 収益性 | 2.6 | 3.8 | **直近 PRODUCTION buy_only 47.6% (CI 下限はランダム未満)** / **calibrator が p17 時代の mapping のまま p20-2 スコアに適用 (rule_version 不整合)** / web 経路のオッズ鮮度が dead constant / P20-2 は buy_only では 2026 悪化 (-3.0pt) |
| パイプライン | 3.6 | 4.2 | **keiba.db (429MB) のバックアップ/復旧手順ゼロ** / raw 再構築でリアルタイムオッズ全損 (ingest 順序) / state.json 非アトミック書込み → 1986 巻き戻りリスク / 部分ファイル非収束の反例経路 |
| コード品質 | 3.9 | 4.0 | is_buy_candidate (金銭直結) に回帰テストなし / Python↔JS キー契約が静かに壊れる (FM-1)。自己反証で「29 dead feature」初回検出を誤検出と訂正 = 証拠規律が機能 |
| 検証プロセス | 3.2 | 3.8 | **weekly_monitor.bat が Task Scheduler 未登録 (schtasks 実測 0 件、CLAUDE.md ルール 4 違反、採用から約 4 週監視不在)** / **calibrator fit 期間 (2025 通年) と 2025val 評価期間が重複 = 「独立 2 dataset」主張の汚染** / monitor.py の baseline がコードパス不一致 |
| **平均** | **3.39** | 4.20 | — |

## v2 監査の評価 (メタ)

v1 が見落としていた実在の問題を 7 名合計で 20 件超検出。特に:
1. 検証監査人が schtasks を実測して**監視が止まっている**ことを発見 (v1 は 3 回のレビューで未検出)
2. 収益性が CI を自力再導出し「2025val 93.1% は CI 下限 23.6% で何も言えていない」「直近 PRODUCTION は 47.6%」を明確化
3. モバイルがコントラスト比を全ペア計算し、v1 で 5/5 だった dark 軸の系統的 fail を検出
4. コード品質が自分の初回検出 (29 dead features) を**自己反証で訂正** — 証拠規律の実効性を実証

## 横断的に見た優先課題 (v2 ベースラインからの改善順)

1. **weekly_monitor の Task Scheduler 登録** (検証) — 即時。CLAUDE.md ルール 4 の既定違反。→ 本 scorecard 保存時に登録実施 (追補参照)
2. **calibrator の rule_version 整合 + p20-2 での再 fit** (収益性 + 検証 + 予想ロジックの 3 名収束) — p17 時代の isotonic mapping を p20-2 のスコア分布に適用し続けており EV の土台が崩れている。fit/eval 期間重複の自動ガードも同梱
3. **dark モードの --grade 上書き削除** (モバイル) — `index.html.j2:50` の 1 行削除で 6 種の 2.29:1 fail が全解消
4. **is_buy_candidate 回帰テスト + オッズ鮮度の filter.py 統合** (コード品質 + 収益性) — 金銭直結ロジックの構造的防御
5. **keiba.db バックアップ手順 + state.json アトミック書込み** (パイプライン)

## 追補

- 課題 1 (weekly_monitor 登録) は本 scorecard 保存と同時に実施。
