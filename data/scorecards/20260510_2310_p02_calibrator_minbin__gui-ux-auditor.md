# GUI / UX 監査人 採点

**改修**: P0-2 (calibrator 過学習対策: `_apply_calibrator` に min_count 未満で raw 寄せ + min_count 20→50) + P0-1 延長 (`gui/app.py:634` の `odds_age > 30` 直書きを `BUY_FILTER_DEFAULT["max_odds_age_min"]` に置換、警告文言を `(>{max_age}分)` で動的化)
**日時**: 2026-05-10 23:10
**対象**: gui/app.py (predictor/rules.py の calibrator 改修は predictor-logic 担当の領域、本監査では UI 表面のみ評価)

## 総合: 3.3 / 5

JS パース確認: `node --check` → CONTROL_HTML 内 JS (12,824 byte、前回比 ±0) は構文エラーなし。総合 1 確定の致命傷は無し。

UX 観点では、本改修の見える部分はオッズ鮮度警告の文言が `(>30分)` 固定 → `(>{max_age}分)` 動的化された点のみ。`config.BUY_FILTER_DEFAULT["max_odds_age_min"]` を変更するとフィルタ閾値とユーザに見える警告文言が同期するようになり、前回 P0-1 でやりかけた「単一出典」がもう一段進んだ。閾値が表示と実挙動でズレる事故 (config を 60 にしても警告は「>30分」と出続ける旧バグ) が消えたので項目 2 を +0.1 微増。calibrator 側の改修は GUI 露出ゼロ (`/predictions` 画面のロジックバックエンド) なので採点に直接寄与しない。前回 3.2 → 今回 3.3 (+0.1)。

## 項目別

- **ボタン発見性 / フロー明示性: 4/5** — 本改修ではボタン構造に触っていない。Ⅰ/Ⅱ/Ⅲ/Ⅳ ステップ + `data-action` 駆動は前回のまま。`<details id="helpBox">` 不在も継続。±0。

- **エラー人間化 / 復旧支援: 3.5/5** — オッズ鮮度警告 `f"オッズ鮮度警告: {odds_age}分前 (>{max_age}分) / 買い候補から除外"` (gui/app.py:636) が config 連動で表示されるようになり、「設定を変えたのにメッセージが古い数値のまま」という違和感バグが消えた。実害は小さいが「人間化」軸では筋の良い改善。`_error_hint` 自体は触っていないので +0.5 まで。

- **進捗表示 / ETA / キャンセル: 3/5** — 本改修は進捗系を一切触っていない。±0。

- **二重実行防止 / ボタン状態管理: 3/5** — 本改修は `_apply_calibrator` の純関数的な数値補正と警告文の f-string 化のみで、`inFlight` / `_run_lock` 周りは未着手。ポーリング経路にも追加負荷なし。±0。

- **レイアウト / タップ領域 / アクセシビリティ: 2/5** — `.sidebar { overflow: hidden }` のまま。input には `title=` も `aria-label` も付与なし。本改修ではこの軸の触り方ゼロ。±0。

## 主な改善提案 (優先 1 件、最大 3 件)

1. **`max_odds_age_min` を input 化して画面から弄れるようにする** — 今回 `BUY_FILTER_DEFAULT["max_odds_age_min"]` 連動はできたが、ユーザがこの値を変えるには config.py を直接編集するしかない。`gui/app.py:1374` 付近の他フィルタ input (`filter_ev` / `filter_kelly` / `filter_top` / `filter_count`) と同列に `filter_max_odds_age` を足し、`get_buy_filter_default` レスポンスにも含めれば、警告閾値もユーザ操作で動かせて UI の自己完結度が上がる。

2. **オッズ鮮度警告に「再取得」ボタンを併設** — `warnings` 配列の該当エントリに対し `detailsBox` 内で `<button data-action="fetch_odds">オッズ再取得</button>` を出すパターンに昇格。今は警告を読んでも次に何をすべきか UI から辿れない (Ⅱ-2 のオッズ取得ボタンに戻る必要がある)。1 クリックで復旧フロー完結 → 「復旧支援」軸を 4 に乗せられる。

3. **(継続) サイドバー overflow と helpBox 追加** — 前々回からの最優先提案がそのまま持ち越し。項目 5 が 2/5 のままだと総合が 3 台前半から動かない。

## 前回からの差分 (前回スコアがあれば)

- ボタン発見性: 4 → 4 (±0) 触り無し
- エラー人間化: 3 → 3.5 (+0.5) 警告文言が config 連動で動的化、表示と実挙動のズレが消失
- 進捗 / ETA / キャンセル: 3 → 3 (±0) 触り無し
- 二重実行防止: 3 → 3 (±0) 触り無し
- レイアウト / アクセシビリティ: 2 → 2 (±0) 触り無し
- **総合: 3.2 → 3.3 (+0.1)** 文言の単一出典化という地味だが確実な改善。calibrator 改修自体は GUI 露出が無いため採点には寄与しない。
