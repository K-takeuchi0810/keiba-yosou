# 採点 2026-05-10 23:30 — P1-3: logging 導入 + サイレント失敗撲滅

**改修内容**:
- `jvlink_client/client.py` の `except Exception: pass` **9 箇所すべて** を `logger.warning(..., exc_info=True)` に置換
- `jvlink_client/ingest.py` の運用パス `print(..., flush=True)` を `logger.error("ingest failed: %s", f.name, exc_info=True)` に置換
- `predictor/rules.py:_apply_calibrator` で calibrator type 異常時に `logger.warning("calibrator disabled: ...")` を追加
- 3 ファイルに `logger = logging.getLogger(__name__)` 導入
- 加えて P0-2 後の長期 backtest を `data/backtest/20260510_232027_tan_p02-calibrator-minbin-all.json` (1,164 戦) に保存

## 総合スコア (項目平均) の推移

| # | 専門家 | 今回 | 前回 | ベースライン | 差分 (前回比) |
|---|---|---|---|---|---|
| 1 | GUI / UX 監査人 | 3.3 | 3.3 | 3.2 | ±0 |
| 2 | モバイル HTML レビュアー | 3.4 | 3.4 | 3.4 | ±0 (3 連続未着手) |
| 3 | 予想ロジック分析官 | 3.6 | 3.6 | 3.4 | ±0 |
| 4 | **収益性 / 投資判断専門家** | **2.0** ⚠ | 2.4 | 1.8 | **-0.4 ⚠ 警告** |
| 5 | データパイプライン技術者 | **4.0** | 3.8 | 3.8 | **+0.2** |
| 6 | コード品質 / 保守性レビュアー | **3.4** | 3.0 | 2.6 | **+0.4** |
| 7 | 検証プロセス監査人 | **4.2** | 3.6 | 3.2 | **+0.6** |

**全体平均: 3.06 (baseline) → 3.14 (P0-1) → 3.30 (P0-2) → 3.41 (P1-3)** = ベースライン比 **+0.35**

## 🟢 改善できた点

- **検証プロセス 3.6 → 4.2 (+0.6)**: 過適合監視 2→4 (改修後 backtest 保存を運用化)、calibration 計測 4→5 (Brier 0.0575 / LogLoss 0.210 が n=16,550 で取れた)
- **コード品質 3.0 → 3.4 (+0.4)**: 観測性 2→4.5 (`except: pass` 9 箇所撲滅 + `print` 運用パスを logger 化)
- **データパイプライン 3.8 → 4.0 (+0.2)**: JV-Link エラー回復 4→5 (サイレント失敗ゼロ、上位水準)

## 🔴 警告: 収益性 2.4 → 2.0 (-0.4)

**P1-3 が後退させたのではなく**、1,164 戦の **長期 backtest が初めて取れたことで、P0-2 直後の「期待値先払い」評価が撤回された** ため。
具体的に発覚した事実:

### 新発見 1: 信頼度ラベルが機能していない
| 信頼度 | 戦数 | 的中率 | 回収率 |
|---|---|---|---|
| 接戦 | 411 | 17.3% | 56.9% |
| 標準 | 323 | 22.3% | 58.3% |
| 混戦 | 339 | 18.6% | **68.3%** |
| **高信頼** | 91 | **28.6%** | **59.8%** ⚠ |

**「高信頼」の回収率が混戦より低い** = 信頼度ラベルが事実上機能していない。

### 新発見 2: フィルタ採用 0 件 / 1,164 戦
`buy_only_bets=0` で **A/B 比較が成立していない**。フィルタが現実分布に対して厳しすぎ。

### 新発見 3: 唯一の光明 — 重賞 G1/G2/G3 で回収率 115.4%
| クラス | 戦数 | 的中率 | 回収率 |
|---|---|---|---|
| **G1/G2/G3** | 52 | 25.0% | **115.4%** 🎉 |
| 条件戦等 | 858 | 21.2% | 60.9% |
| L/OP 特 | 252 | 14.7% | 50.0% |

中山 91.4% / 京都 80.3% も黒字寄り。**「重賞ホワイトリストモード」が次の最有望路線**。

## 横断的な次の優先課題 (Top 3)

### 🔴 P0-3 (新規・収益性 後退対応). 重賞ホワイトリスト + 「高信頼」定義の再構成
- 指摘元: 収益性 / 投資判断専門家
- `BET_WHITELIST_MODE=1` で「`grade_code in [A,B,C,F]`」「track_code in [07,09]」のみ買い対象
- `_value_score` ベースの「高信頼」を破棄し、backtest で 30%+ 的中が確認できた合成条件 (重賞 + 札幌/東京/中山) で再定義

### 🟠 P1-1 (継続). `_score_one` 491 行を分割 + 直書き 60 箇所を weights.json + dead feature 6 件削除
- 指摘元: 予想ロジック分析官 + コード品質レビュアー
- 4 連続未着手

### 🟡 P2-1 (3 連続未着手). モバイル ダークモード CSS 変数化 + `<details>` インジケータ復元
- 指摘元: モバイル HTML レビュアー
- 3 改修連続でモバイルが据え置き

## 残課題 (各専門家の指摘から)

- **GUI**: logger を GUI から見られるようにする (`logging.basicConfig(filename='data/logs/app.log')` + 「ログを開く」ボタン)
- **データパイプライン**: 3 連続持ち越しの mtime / JVStatus timeout / DB PRAGMA を着手
- **コード品質**: `dead feature` 6 件削除 (項目 2 が 2/5 で固定の主因)
- **検証プロセス**: walk-forward 評価モード (`--holdout-from`)、reliability diagram PNG 出力

## 個別 scorecards

- `20260510_2330_p13_logging__gui-ux-auditor.md`
- `20260510_2330_p13_logging__mobile-html-reviewer.md`
- `20260510_2330_p13_logging__prediction-logic-analyst.md`
- `20260510_2330_p13_logging__profitability-judge.md`
- `20260510_2330_p13_logging__data-pipeline-engineer.md`
- `20260510_2330_p13_logging__code-quality-reviewer.md`
- `20260510_2330_p13_logging__validation-process-auditor.md`
