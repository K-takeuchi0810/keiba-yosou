# 予想ロジック分析官 採点

## 総合: 3.6 / 5 (前回 3.6 → 3.6, ±0)

## 項目別

- **シグナル網羅性: 4/5** — `predictor/` 配下 (rules.py / features.py / weights.json) に変更なし。シグナル層は前回どおり維持。
- **重み妥当性 / 過適合リスク: 3/5** — `weights.json` / `calibrator.json` ともロジック側の編集なし。今回の改修は買い候補の後段 race ホワイトリストのみで、過適合経路を増減しない。magic number 直書き繰越も同じ。
- **信頼度判定 / 確率推定: 4.5/5** — `_apply_calibrator` / `_score_probabilities` / `_confidence` いずれも変更なし。前回 P1-3 で入った `calibrator type 異常時 warning` もそのまま。素点維持。
- **デッドコード / 設計の整合性: 3/5** — `_score_one` 491 行肥大 / dead feature 4 件 / dead weight 2 件 / magic number 50+ 箇所、すべて未着手。今回の P0-3 ホワイトリストは予想ロジック外 (買い候補側フィルタ) に閉じているので整合性は乱していないが、設計負債は据え置き。
- **本番運用との乖離リスク: 3/5** — 重賞ホワイトリスト導入は「買い対象を限定する」方向なので予想ロジック自体の本番乖離は不変。`leg_quality_*` のサイレント失敗・全 bin 恒等寄せの未検知も前回どおり残置。

## 主な改善提案 (優先 1 件、最大 3 件)

1. **【繰越】前回優先 #1 と統合した一発ログ化** — `_apply_calibrator` の `logger.warning` を呼び出しごと発火から `_load_calibrator` の `mtime` キャッシュキーに同期した once-only に変更し、同時に「有効 bin=N / 恒等寄せ bin=M / 空 bin=K / `min_count`=50」を `logger.info` 1 行で集計出力。実装 < 5 行で「ログ氾濫」と「全 bin 恒等寄せのサイレント失敗」を同時解決。
2. **【繰越】`_score_one` 491 行の責務分割 + magic number 50+ 箇所の `weights.json` 移管 + dead feature/weight 削除** — 前回優先 #2 そのまま。今回も対象外。
3. **【繰越】`min_count` 決定根拠コメントと空 bin の除外** — 前回優先 #3 そのまま。今回も対象外。

## 前回からの差分

- シグナル網羅性: 4 → 4 (±0) 維持 (変更なし)
- 重み妥当性 / 過適合リスク: 3 → 3 (±0) 維持 (変更なし)
- 信頼度判定 / 確率推定: 4.5 → 4.5 (±0) 維持 (変更なし)
- デッドコード / 設計の整合性: 3 → 3 (±0) 維持 (繰越課題そのまま)
- 本番運用との乖離リスク: 3 → 3 (±0) 維持 (変更なし)

## 補足

P0-3 重賞ホワイトリストは予想ロジック (`predictor/`) の担当範囲外で、後段の買い候補フィルタとして実装されている。よってロジック分析官の観点では加点も減点もなく総合 3.6 維持。前回優先 3 件 (once-only ログ統合 / `_score_one` 分割 + magic number 移管 / `min_count` 根拠コメント) は **3 回連続で繰越** になっており、次回以降いずれか 1 件でも着手すれば素点 4 へ届く。ホワイトリストのような後段フィルタが効くほど、上流の予想ロジック側が「買って良い候補を粒度高くスコアリングする」前提になっているので、繰越課題のうち magic number 移管と dead feature 削除は優先度を上げて取り組む価値が高い。
