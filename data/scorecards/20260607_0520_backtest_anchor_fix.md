# 採点 2026-06-07 05:20

**改修内容**: 「直近バックテスト」全0表示バグ修正 (集計起点を to_date→直近確定日 anchor に変更) + 誤読防止のラベル整直化 (本命単勝/フィルタ非適用の注記、日付を .num スロットから caption へ)
**対象ファイル**: gui/app.py (_recent_backtest, backtest カード JS/CSS)

## 総合スコア (項目平均) の推移

| 専門家 | 今回 | 前回 | 差分 |
|---|---|---|---|
| 検証プロセス監査人 | 3.8 | 4.0 | -0.2 |
| GUI / UX 監査人 | 4.1 | 4.1 | ±0 |
| 収益性 / 投資判断 | 3.5 | 3.5 | ±0 |
| コード品質 / 保守性 | 4.2 | 4.3 | -0.1 |
| 予想ロジック分析官 | 4.35 | 4.35 | ±0 (ドメイン未変更, carry) |
| データパイプライン技術者 | 4.0 | 4.0 | ±0 (ドメイン未変更, carry) |
| モバイル HTML レビュアー | 4.6 | 4.6 | ±0 (ドメイン未変更, carry) |

**後退項目: なし (いずれも 0.3 未満)。** 検証 -0.2 / コード品質 -0.1 はレビュー時点の指摘だが、その主因 (ラベル誤読・日付の .num 流し込み) を **本セッション内で修正済**。
レビューは関連 4 名 (validation/gui-ux/profitability/code-quality) のみ実行。残り 3 ドメインは前回 full run から diff ゼロで carry forward。

## 経緯と各専門家の所見 (要約)

### 発端 (ユーザ報告)
「直近バックテスト」が全0 → 「実行されていないのでは」。

### 調査結果 (実測)
バックテストは正常稼働。原因は集計窓が `to_date`(=予想対象日=当日) 起点だったこと。DB の確定済みは最新 2026-05-31 で、当日 06-07 近辺は未確定。窓に確定レースが入らず:
- 直近3日/7日/当月 → 対象0 → 全0、直近30日のみ 340 レースで実数が出ていた。

### 修正
集計起点を **「to_date 以前で最新の確定開催日 anchor」** に変更 (`MAX(date) WHERE confirmed_order>0 AND date<=to_date`)。実測 (to_date=20260607): 3日→48 / 7日→48 / 30日→340 / 当月→340 レース。payouts に 0530-0531 の 48 行存在を確認、回収率も算出可。

### 検証プロセス監査人 — 3.8 / 5 (4.0 → -0.2)
anchor 方式は時系列リーク防止 **5/5** (確定済み∧to_date以前のみ、未来参照ゼロ)。全0=偽陰性バグの解消も評価。ただし「rule-only・全レースべた買い・フィルタ非適用」の文脈がラベルに出ないままだと買い候補戦略と回収率が混同される誤読リスク (calibration/開示の弱さ) を指摘 → **本セッションで note 追加し対応**。残: n<30 時の return_rate 過信ガード・anchor 鮮度警告。

### GUI / UX 監査人 — 4.1 / 5 (±0)
全0で「壊れている」と見えたカードが確定成績を返すことで機能不全の誤認を解消。指摘「bt.label の長い日付列が数値強調 .num スロットに流し込まれ折返す」→ **本セッションで日付を .bt-period caption に移し、.num には対象R(件数)を表示** して対応。発見性 (3) の天井は title/helpBox/input既定値の **4 連続宿題**。

### 収益性 / 投資判断 — 3.5 / 5 (±0)
「買いフィルタ整合化はすべきでない (ロジック健全性センサーは母集団が広い方が安定 + filtered backtest と二重持ちは単一出典違反)、ラベル正直化が正解」と明言 → **note 追加で対応**。回収率本丸 (控除率80%未突破) は未変更で総合 3.5 維持。次回ラベル対応で EV 項 +0.5 見込み。

### コード品質 / 保守性 — 4.2 / 5 (4.3 → -0.1)
anchor 起点設計・根拠コメント・早期return・strptime例外防御は丁寧。減点は DRY: predict_race 集計ループ (top抽出→ヒット判定→payout) が scripts/backtest.py 本体と構造重複 (I/O プリミティブは import 共用)。`tally_top_pick` helper 切り出しを中期・別タスクで推奨。

## 横断的に見た優先課題

1. **(対応済) backtest カードの誤読防止** — note「本命単勝・全レース/買いフィルタ非適用の参考値」追加 + 日付を caption 化 + 対象R を件数表示。3 レビュアー共通の最優先指摘を in-session で解消。
2. **n 少時の return_rate ガード + anchor 鮮度警告** (validation/gui-ux) — total<30 で回収率をグレーアウト/「n少」、anchor が to_date より N 日前なら「確定最新 MM-DD」を明示。
3. **input 既定値の config 注入** (gui-ux, 5 連続宿題) — 着手の好機。
4. **backtest 集計ループの helper 共用化** (code-quality, 中期) — scripts.backtest に tally_top_pick を切り出し GUI と共用。top 抽出基準 (rank==1&mark vs preds[0]) の統一も。

## 検証ログ
- `python -m py_compile gui/app.py` → PASS
- 埋め込み JS 抽出 → `node --check` → PASS
- anchor=20260531 を実 DB で確認、各レンジの対象レース (48/48/340/340) と payouts 48 行を実測
- モック描画で period caption / 対象R=48 / 回収率92.4% / note 表示を DOM eval + screenshot 確認
