# 検証プロセス監査人 — 20260822_1210 fresh_odds_recovery

**判定**: HOLD (数値の正本化と解除判断を保留。サスペンド発効自体は止めない)
**総合**: 3.4 / 5 (前回 4.0、-0.6 ⚠ 後退)

| 項目 | 今回 |
|---|---|
| バックテスト設計 | 4 |
| 時系列リーク防止 | 3 |
| calibration / 答え合わせ計測 | 3 |
| A/B・再現性・実験ログ | 3 |
| 監視・ドリフト検知・運用ゲート | 4 |

**HOLD 3 事由**:
1. `BET_FILTER_IGNORE_SUSPENSION` が backtest の env_overrides 追跡キーに未登録 → **是正済み**
2. 解除機構に指定された `filter_sweep --recent-3fold` にオッズ鮮度ゲートが無く、かつ
   repair --apply 前の DB で実行中 = **走行中 run の結果は解除判断に使用不可**
   → 是正済み (ゲート移植 + `--no-odds-gate` + テスト)。走行中 run は破棄した
3. サスペンド根拠が修復前の縮小母数 (新規窓 648 中 323 除外) 由来 → 修復後の再ベースライン待ち

**独立再導出**: 両 JSON の数値は一致確認。2 run の合算 (1,893 戦) を撤回した判断は正しい
(窓が重複しており加算は二重計上)。答え合わせは判定者の再集計と完全一致せず、境界・除外規約の
文書化を要求 → コミットメッセージに集計定義を明記。

**評価された点**: 6 日間無音障害の根本 3 点 (ハング / ログハンドル / 無通知) を各々潰しており
事故因果とコードが一致。repair の before_rows 監査ログ (巻き戻し可能) は良設計。
サスペンドは同一 run 内の subset 比較で期間ズレ・コードパスズレなし。

**指定された正しい順序**: repair --apply → 同 2 窓の backtest 再実行 → monitor baseline 再凍結 →
鮮度ゲート付き sweep。この順以外は baseline が即陳腐化する。

**over-claim 指摘**: 「価値破壊が 2 期連続で確認された」は CI 重複下で不成立。
「点推定で 2 期連続下回り + 便益の証拠なし」が正確 → config のコメントを是正済み。
