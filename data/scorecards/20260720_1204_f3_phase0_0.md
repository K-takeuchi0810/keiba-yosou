# 採点 2026-07-20 12:04

**改修内容**: F3 Phase 0.0の事前登録3リークチャネルを固定split/seedで定量化し、安全境界と再現情報を付与
**対象ファイル**: `scripts/f3_phase0_0_eval.py`, `tests/test_f3_phase0_0_eval.py`, `docs/F3_phase0_0_result.md`, `data/f3_phase0_0/metrics.json`

## 総合スコア（項目平均）の推移

| 専門家 | 今回 | 前回 | 差分 |
|---|---:|---:|---:|
| GUI / UX 監査人 | 3.6 | 3.6 | 0.0 |
| モバイル HTML レビュアー | 4.0 | 4.4 | **-0.4 ⚠ 後退** |
| 予想ロジック分析 | 4.0 | 4.1 | -0.1 |
| 収益性判定 | 2.0 | 3.8 | **-1.8 ⚠ 後退** |
| データパイプライン | 4.1 | 4.0 | +0.1 |
| コード品質 | 4.0 | 3.9 | +0.1 |
| 検証プロセス | 4.4 | 4.6 | -0.2 |

警告対象は2件。ただしモバイルの低下は既存WCAG/safe-areaの再測定補正で今回差分による回帰ではない。収益性の低下はproduction改修による悪化ではなく、425 betの固定OOSで62.09%（日block 95% CI 48.97%–76.28%）という強い負の証拠へ評価を更新した結果である。

## 各専門家の所見

### GUI / UX 監査人

- GUI差分なし、Python展開後の埋込みJSは `node --check` 成功。F3起因のGUI回帰なし。
- 現行GUIの優先課題は長時間処理のキャンセル保証とARIA補完。判定HOLDは既存課題への絶対評価で、F3の停止条件ではない。

### モバイル HTML レビュアー

- `web/` 差分なし、HTML再生成成功、外部リソース依存・無効馬番なし。F3起因の表示回帰なしでPASS。
- 既存の枠番4/6/7/8の色コントラストとsafe-area未対応を絶対基準で再評価したため前回比-0.4。

### 予想ロジック分析

- M0/M1は同一public model/validationで3列だけzero、M2は同一split/seed/rounds/paramsで112→109列のexact-3除外を確認。
- control−treatment AUC差+0.002539、race top-1差0。`same_day_gate_bias_score`やrule側のsame-day経路が残るため、全リーク除去・真baselineという因果表現は不可。報告を「事前登録3チャネル差」へ修正済み。

### 収益性判定

- 固定OOSは425 bets / 65 hits / 払戻26,390円 / 回収率62.09% / 損失16,110円。日block CI上限76.28%も80%未満で実弾投入不可。
- 旧70.70%は1620 races / 199 bets、今回は1578 races / 425 betsでrules SHAとCI単位も異なるため、-8.61ptを3チャネルのROI寄与とは解釈できない。

### データパイプライン

- read-only DB、production 4 artifact SHA不変、cache実race日付のsealed/登録窓外拒否、プロジェクト外path拒否、historical calibrator必須化とSHA記録を確認。
- 初回3.7から指摘対応後4.1へ改善。残課題は出力のatomic化、実験artifact hash manifest、82.9分OOSのresume/cancel。

### コード品質

- 373 passed / 4 skipped、対象7 tests、構文・契約検査成功。script/calibrator SHA、fail-closed境界、finally復元を確認。
- 初回3.7から指摘対応後4.0へ改善。残課題は仕様値の二重記述と長い `run()` の責務分割。

### 検証プロセス

- 固定split/seed/params、exact-3 ablation、sealed分離、production不変は強い。validation上の事前登録3チャネル差は再現可能。
- OOSに同一現行stackのM2-controlがなく、validation差にもrace/day cluster CIがないため、ROI寄与と統計的不確実性は未確定。

## 横断的に見た優先課題

1. **同一OOSでM2-control/treatmentをpaired評価**（収益性 + 検証プロセス）
   - race/payout universe・rules・calibrator・filterを固定し、bet ledgerと開催日blockのpaired ROI差95% CIを保存する。
2. **validation差のcluster不確実性と校正を追加**（予想ロジック + 検証プロセス）
   - race/day block CI、reliability bins、M2専用TRAIN-only calibratorを追加し、差の実用的・統計的意味を判定する。
3. **実験provenanceと長時間実行の堅牢化**（データパイプライン + コード品質）
   - 全成果物SHA manifest、atomic write、resume/cancelを追加し、F3一式をcommitで固定する。

