# 採点 2026-07-16 23:02 (正規版)

**改修内容**: 第 4 サイクル仕上げ — cleanup 述語一元化 + 過去日第 2 述語 (カナリア完成) / 品質ゲート取消偽陽性是正 (`max(starter_count, registered_count)`) / 通知・staging 堅牢化 5 点 / テンプレート data 属性化 (3 サイクル繰越のクローズ)
**対象 commit**: `494a1e9` (Codex 実装、指示書 `docs/codex_fix_20260716_cycle4_polish.md`) + `1309119` (レビュー是正 2 件、Claude) + `2f76e3c` (テスト時限爆弾修正、Claude)、branch `codex/cycle4-polish`。Codex の scorecard 自作ゼロ継続

> **サイズゲート未達の監査**: Codex は「dist +50KB 以内」の受入基準未達 (+104,785 bytes) を正直申告。
> mobile-html-reviewer と validation-process-auditor が独立に paired 実測し、増分は行数線形の属性コスト
> (1,955 行 × ~54B、申告値と 1 byte 単位で一致)、本番公開ペイロード (単日 428,798 bytes / +25KB) は
> 予算内 — **「測定条件 (±14 日開発窓) がペイロードを代表しないゲート仕様の欠陥」と判定、
> post-hoc weakening に非該当** (アーキテクチャで固定された事実)。属性の絞り込みは不要。
> 前回の正規基準は `20260716_2104_alerting_and_hardening.md`。

## 総合スコア (項目平均) の推移

| 専門家 | 今回 | 前回 (2104) | 差分 | 判定 |
|---|---:|---:|---:|---|
| GUI / UX 監査人 | 4.2 | 4.0 | +0.2 | PASS |
| モバイル HTML レビュアー | 4.4 | 4.6 | -0.2 (dist 既存債務の悪化計上、視覚回帰ゼロ) | PASS |
| 予想ロジック分析官 | 4.4 | 4.6 | -0.2 (後退でなく新実装分の留保計上と明記) | PASS |
| 収益性ジャッジ | 4.3 | 4.2 | +0.1 | PASS |
| データ基盤エンジニア | 4.7 | 4.3 | +0.4 (是正後の再判定) | PASS |
| コード品質レビュアー | 4.1 | 4.2 | -0.1 (是正後の再判定) | PASS |
| 検証プロセス監査人 | 4.4 | 4.4 | ±0 | PASS |
| **平均** | **4.36** | 4.33 | +0.03 | **7 PASS** |

- 0.3 以上の後退なし。前回 7 名の実行可能提案の消化率 ~100% (validation 実測)。
- **マージ前是正 2 件を実施 → 指摘 2 名が再検証で承認**:
  1. `--dry-run --execute` 併用時に「削除しない」と印字しながら削除する虚偽安全表示 (code-quality 発見)
     → dry-run 常時優先に修正、回帰テスト付き。再判定 4.0→4.1 PASS
  2. cleanup inspect の日付非対応 — 未来日 pre-draw 72 行を false-violation 計上し週末の是正を
     ブロック (data-pipeline が live DB で実測) → monitor と同じ日付規律に一元化 + **削除スコープを
     「正規行と共存する '00'」に限定** (過去日単独は manual judgment abort = 再取得が正解の可能性)。
     live DB 再実測で false-violation 0 / 検知力保持を確認。再判定 4.5→4.7 PASS「提案より安全側で正しい判断」

## 主要な確定事項

- **'00' 問題は 6 層防御で構造的に完結** (予想ロジック分析官)。過去日第 2 述語の strict `<` 境界は
  「開催日朝の実行順依存 FP を構造的に排除する正しい仕様」と実測付きで是認。
- **取消偽陽性 = 封印ホールドアウト欠測日リスクの消滅** (収益性ジャッジ)。取消は 2,214 レース中
  126 件 (5.7%) で恒常発生し、旧ゲートは欠測日を系統的に生む構造だった。registered−starter の
  実乖離は最大 3 で、ゲートの緩みは無視可能。残存偽陽性は registered 欠損 0.8% のレースのみ。
- **モニタ↔cleanup の永久非収束ループ解消** (GUI)。「状態が嘘をつく系」の構造解消 2 件。
- **contract test によるテンプレート↔パーサ契約の固定** (全員)。旧形式アーカイブ HTML の
  fallback 互換は 3 名が実 HTML (470 頭/169 件/1-17) で独立再実測。

## 各専門家の所見 (要点)

### GUI / UX 監査人 (4.2 / PASS)
- 提案消化 3/3。gap 通知の詳細埋め込み・monitor 警告の coexist/past_only 内訳・品質ゲートの根拠付きエラーで「通知だけで診断が完結する」方向へ。
- 提案: archive 失敗通知への文脈 (日付/パス)、staging の stale status 日付照合、gap 通知の 1 メッセージ結合。

### モバイル HTML レビュアー (4.4 / PASS)
- data 属性化は視覚デルタゼロ (`.col-odds` への CSS 参照 0 件)、autoescape・欠損時省略・fallback とも健全。「表示あり属性なし」の乖離が構造的に発生しない同一条件ゲートを評価。
- サイズ判定: 上記のとおり測定条件の欠陥と判定。**GUI 手動 publish で 1.76MB が iCloud に乗る経路は既存債務として残存** (窓明示 or 予算超過時 publish 拒否を提案)。
- 理論上の反例 1 件: 不正な data 属性値 (非数値) では fallback が抑止され odds=None に落ちる — 自前 generator は数値のみ出力のため実害経路なし (parse 成功時のみ抑止フラグを立てる是正を提案)。

### 予想ロジック分析官 (4.4 / PASS)
- **6 層防御の棚卸しで「構造的に完結」と判定**。述語単一出典性は grep 全数で 5/5 (残る '00' リテラルは全て削除系の仕様)。
- 残る既知ギャップ 3 点 (いずれも低リスク): 当日残置の検知遅延 ≤1 日 (仕様)、`excluded_placeholder_rows` カウンタの '00' 限定 (過小報告のみ)、非 '00' invalid の手動復旧。
- 提案: 等号境界 (race_date == today) の固定テスト、backup 照合と abort 条件の暗黙結合へのコメント。

### 収益性ジャッジ (4.3 / PASS、+0.1)
- 取消偽陽性の消滅を実データ再導出で確認 (上記)。「品質ゲートは正当なドメイン事象でなく不可能状態にのみ発火させる」原則に合致。
- 段階判定: **観察用のまま**。本改修は封印ホールドアウト判定 (1 回限り) の分母を守るインフラ。
- 提案: registered 欠損時の上限を starter+3 に / staging の日付整合チェック / past_only 境界の TZ 文書化。

### データ基盤エンジニア (4.7 / PASS、+0.4 — 再検証後)
- 前回提案 3 件 (過去日 FN 補完 / read-only 化 / except 拡大) の充足を実測確認。bat の errorlevel 伝播は精読で非破壊を確認。staging fallback の設計 (stale status 到達は publish skip 時のみ = 冪等 no-op) を精査し健全と判定。
- 是正後再実測: cleanup の false-violation 0、`today='29991231'` 注入で検知力 72 を保持。
- 残提案: 監視/公開 read 経路の残り 2 箇所 (`build_daily_results.py:508` / `auto_predict.py:88`) の ro 化、staged archive の日付整合検証。

### コード品質レビュアー (4.1 / PASS — 再検証後)
- 前回提案 3 件すべて構造的に充足。contract test を「言語境界契約の構造的固定」と評価。data 属性 fallback の失敗モードは「静かな欠測ではなく contract test の失敗として顕在化する fail-loud 設計」と分析。
- 発見 → 是正済み: --dry-run/--execute 競合 (虚偽安全表示)、テスト時限爆弾 (2027 年赤化、`2f76e3c` で修正)。
- 繰越 (軽微): `SQL_VALID_HORSE_NUM.replace(...)` の文字列再発明 → `sql_valid_horse_num(col)` 関数の使用、日付規律の 2 ファイル並立 (`_coexist_exists` の db.py 移設)、`excluded_placeholder_rows` の述語統一、staging fallback の無ログ。

### 検証プロセス監査人 (4.4 / PASS)
- 全主要数値を独立再導出して一致 (paired 実測で属性増分が申告値と 1 byte 一致)。3 サイクル繰越は「実施側で執行」と確認。
- 取消ゲートは境界の両側を上限ちょうどでテストしており設計妥当。第 2 述語の当日境界は自ら実測 (当日単独 '00' 非検知 = 安全) — スイートへの固定を提案。
- **週末検収 4 項目との干渉なしを diff レベルで確認** — 7/18-19 の検収は前回固定の合否基準のまま実施可能。
- 教訓 (指示書テンプレ改善): サイズ/件数系の受入ゲートは測定コマンド・render 窓・baseline を仕様に明記すること (今回の未達は窓未指定が原因)。

## 横断的に見た優先課題 (次サイクル)

1. **述語基盤の db.py への完全集約** (code-quality + prediction + pipeline)
   - `_coexist_exists` と日付規律を db.py へ移設し、`sql_valid_horse_num(col)` 関数を cleanup でも使用。
     `excluded_placeholder_rows` (build_daily_results.py:510) の述語統一。等号境界・当日境界の固定テスト追加。
2. **観測の残渣** (gui + pipeline + code-quality)
   - archive 失敗通知への文脈埋め込み / staging fallback の WARN ログ + 日付整合検証 /
     read 経路残り 2 箇所の ro 化 / gap 通知の 1 メッセージ結合 / coverage クラッシュ時の通知欠落。
3. **dist 予算の恒久解** (mobile — 既存債務、type-D)
   - GUI 手動 publish の窓明示 or 予算超過時 publish 拒否 (1.76MB が iCloud に乗る最後の経路)。
     不正 data 属性値での fallback 抑止是正 + 省略ケースの contract 固定。

## 検収 (7/18-19 初実戦) — 前回固定の 4 項目のまま実施

①gap 誤警報 0 / ②新 manifest に schema:2 + post_start_unclassified_rows / ③アーカイブ自動 commit + sha 非 null / ④Discord 実着弾 ≥1 (best-effort につき未着弾でも predict exit 0 なら可)
