# 採点 2026-06-12 01:05

**改修内容**: P21 GUI 全面改善 (commit 3de1381) — ノートPC対応ウィンドウ縮小 (1240x690+min_size) / プレビュータブ統合 (PREVIEW_HTML 廃止・file:// 配信) / 予測+バックテスト キャッシュ (41.8s→0.67s / 105s→0.02s) / 日付プリセット / フィルタリセット / 更新中インジケータ / ライブ天候併記
**対象ファイル**: gui/app.py, .claude/skills/python-embedded-js/SKILL.md

## 総合スコア (項目平均) の推移

| 専門家 | 今回 | 前回 | 差分 |
|---|---|---|---|
| GUI / UX 監査人 | 4.2 | 4.1 | +0.1 |
| モバイル HTML レビュアー | 4.8 | 4.8 | ±0 |
| 予想ロジック分析官 | 4.4 | 4.4 | ±0 |
| 収益性 / 投資判断 | 3.4 | 3.5 | -0.1 |
| データパイプライン技術者 | 4.0 | 4.0 | ±0 |
| コード品質 / 保守性 | 3.9 | 4.0 | -0.1 |
| 検証プロセス監査人 | 3.8 | 3.8 | ±0 |
| **平均** | **4.07** | **4.07** | **±0** |

-0.3 超の後退なし。

## 各専門家の所見

### GUI / UX 監査人 (4.2)

- ボタン発見性 4 (+1): プレビュータブ常設化 + 日付プリセット + フィルタリセット + ツールバーメタ
- エラー人間化 4 (±0): input 既定値ハードコード (4 セッション宿題) を API 注入で解消。残: venv64 hint 欠如
- 進捗/キャンセル 4.5 (+0.5): キャッシュで待ち時間自体を消滅 + seq ガード + 即時フィードバック。**残: _run_render_in_venv64 の subprocess に中止が届かない (最長ステージで中止無効)**
- 二重実行防止 4 (±0): TOCTOU 窓 + Python 側 running ガード無し (持ち越し)。新規軽微: _predictions_cached の stale 書き戻しレース
- レイアウト/a11y 4.5 (±0): 1240x690 + ブレークポイント整合は模範的。aria 不在継続

### モバイル HTML レビュアー (4.8)

影響なし判定。`_gui/control.html` は publish_to_icloud 対象外で iPhone に漏れない (検証済)。再生成 981KB 健全。
提案: (繰越) buy-board summary 44px / **placeholder index.html に viewport メタ** / HTML サイズ監視

### 予想ロジック分析官 (4.4)

キャッシュの責務分離 (予測=期間キー / フィルタ=毎回評価) は模範的。GUI 内発の DB 変更は invalidate 全数カバー。
残: R1 GUI 外部 DB 書込みの非検知 / R2 publish の過剰 invalidate / R3 共有ミュータブル参照 (実害なし)。GATE 問題なし。

### 収益性 / 投資判断 (3.4) ⚠ 重要指摘

- **買い目フィルタ 2.5 (-0.5)**: 「config 一本化」は未完 —
  1. `options()` の JS フォールバック 1.05/0/10/20 が残存。現 config は 4 値 None なので**幻の EV≥1.05/Odds10-20 制約が常時送信される**
  2. `bet_filter` 4 キーのみを filter_spec として渡すため **min_kelly=0.05 (P15 主絞り) と max_predicted_p=0.40 (S5-3 防御) が脱落** → GUI の買い候補 ≠ backtest buy_only 93.1% の集合
  3. 「既定値に戻す」は現 config (None) では実質 no-op (applyBuyFilter が null skip)
- キャッシュの odds_age ガードは fail-safe 方向で正しい (新規リスクなし)
- **GUI を見て実弾投資する前に bet_filter merge 修正が必須**

### データパイプライン技術者 (4.0)

無効化を開始時に置く設計は失敗時安全側で正しい。実測 60 倍超の改善は本物。
穴: (a) **実行中のフィルタ/期間操作で部分取込み DB からキャッシュ再充填 → 完了後も生存** (b) GUI 外部変更の非検知。
提案: 完了時 invalidate / DB mtime をキー混入 / 実行中 UI disable

### コード品質 / 保守性 (3.9)

- dead code 5.0 (+1.0): PREVIEW_HTML 一族 5 シンボル完全削除、新規シンボル全消費
- DRY 3.5 (-0.5): buy filter の config↔JS 平行記述が「常時発火する stale 値」と判明
- magic 3.5 (-0.5): limit=100 / 600s / 1000ms 裸定数
- 提案: フィルタ単一出典化完遂 / silent catch 可視化 / キャッシュ世代カウンタ

### 検証プロセス監査人 (3.8)

計算ロジック無変更・anchor 維持・note 表示経路生存確認。リーク防止 5/5。
新規 stale 窓 2 件: 実行中 range クリック → stale キャッシュが完了後も生存 / refreshBacktest に seq ガードなし。
**n<30 ガード + anchor 鮮度警告は 3 回目の持ち越し** (次回未着手なら calibration 項を 2 に下げる宣言)。GATE: PASS。

## 横断的に見た優先課題

1. **買い目フィルタ単一出典化の完遂** (担当: profitability-judge + code-quality-reviewer)
   - `get_dashboard` の bet_filter を `dict(BUY_FILTER_DEFAULT)` ベース merge に / JS フォールバック 1.05/0/10/20 撤去 (空 input は送らない) / applyBuyFilter の null クリア
   - GUI の買い候補が採用戦略 wl_kelly_ge_05 (backtest buy_only 93.1% を検証した集合) と一致するようになる。**実弾運用の前提**
2. **キャッシュ stale 窓の閉鎖** (担当: data-pipeline + validation + gui-ux + code-quality の 4 名収束)
   - DB 書込みアクションの完了時にも invalidate / 世代カウンタで in-flight 書き戻しを破棄 / refreshBacktest に btSeq ガード
3. **中止ボタンを venv64 subprocess に届かせる** (担当: gui-ux-auditor)
   - `_run_render_in_venv64` を Popen + poll ループ化し CancelledError で kill。「中止が見えるのに最長ステージで効かない」解消
