# 検証プロセス監査人 採点 — 99eec68 Phase 0.5-4B 基盤修復

## 判定: HOLD

**理由**: 前回の降格宣言 2 件 (検出力の事前記載 / skew 方向の訂正) は **両方とも執行済を確認**、0.5-4A の凍結・paired 再評価・race 単位 bootstrap・provenance は監査に耐える。しかし修復で追加した `h_history_truncated` が **学習 24.3% (2022 年は 40.9%) → 2026 評価 1.4%** と時間単調にドリフトする特徴になっており、**「観測窓の経過時間を数える特徴を除く」という今回の修復目的と同じ欠陥クラスを新たに土台へ持ち込んでいる** (2021-2025 だけで検出可能だったのに未報告)。加えて `SPLIT_DIVERGENCE` は宣言であって強制ではなく、モデル meta の学習窓と評価窓の重なりを実行時に落とす仕組みが無い。2026 を再消費せず是正できるため FAIL ではなく HOLD。
**改修タイプ**: type-B/C 混成。P25 固有ゲートは N/A。
**次アクション**: (a) `h_history_truncated` の train/serve 分布差を実測で追記し、Group A 着手前に「残す / 外す」を **2021-2025 の情報だけで決定**、(b) 評価側に「booster meta の train・validation 窓と評価窓が重ならない」実行時 assert、(c) 4B の検証 ΔLogLoss に race bootstrap CI、(d) 2026 窓の消費回数台帳 (0.5-3 / 4A×3 / 4B×1) を 1 箇所に集約 (**2 回目の未執行**)。

## 総合: 3.4 / 5 (前回 3.6 → −0.2)

## 項目別

- **バックテスト設計の正しさ: 4/5** — 4A/4B は同一窓・同一集合 (analysed 931 / fresh 626 / mismatch 6)・同一 eval コード、`label` で工程を刻印。「欠陥が消えたか」を主目的に据え性能比較を副次化した設計は指示どおり。減点: 2026 窓を **4 回目**消費したことの多重観察会計が無い。
- **時系列リーク防止: 3/5** — ①同日リーク: 監査 SQL を `build_dataset` の包含規則と逐条照合 — 場 1-10 / `horse_num` 除外 / `data_div<>'9'` / `abn∉{1,2,3}` / 信頼下限 / 365 日境界 / 同日は `start_time <` すべて一致。**不一致 1 点**: `build_dataset` は `finished_races` に無いレースをカウンタからも除くが監査 SQL は除かない。該当は 2026-09-20/21 の 48 レース (窓内 0 行) なので今回の「0 / 2,000」は有効だが、**将来窓で偽陰性**。また検査は **調教師カウンタのみ・2026 窓のみ** (0.5-3 で 31.4% 混入した父、騎手・馬は未検査)。②教師混入なし。③ **train-serve skew (新規)**: `h_history_truncated` の発火率を DB から独立再計算 — warmup 82.4% / 2022 40.9% / 2023 22.3% / 2024 9.7% / 2025 3.9% / 2026 1.4%。学習では「信頼下限の打ち切り」、2026 では「地方・海外からの転入」と **意味が違う集団に同じ 1 が立つ**。学習 2022 行の 41% は馬の通算カウンタが打ち切られたまま = **burn-in 1 年では馬レベルの累積は直っていない**。「累積生涯カウントは作らない」規律が人的カウントにしか適用されていない。④評価窓は門を通過。
- **calibration / reliability: 3/5** — 帯別較正は継続出力。減点: 検証 ΔLogLoss (0.20360 → 0.20348) は点推定のみで CI 無し。RESULTS の「N=94,709」は **旧モデルの数値**で、新モデルの根拠は N=47,715・CI 無し。
- **A/B / 再現性 / 事前固定: 4/5** — **凍結は成立**: commit 版 4A JSON と frozen は EOL 差を除き byte 一致、HEAD でも 4A JSON は md5 不変。旧モデルは `e93e3f9` から復元可能と明記。**「2026 を見ずに決めた」**: 中間 commit が無く時系列は git では検証不能だが、`dirty_paths` が工程順を復元する (audit → 修正 → audit → 再訓練 → 1 回評価)。代替窓・別閾値の成果物は tree・scratch・stash・reflog に **痕跡なし**。反証不能だが矛盾証拠も無し。留意: `_after.json` は 2026 窓の **特徴分布**を含む (成績は見ていない)。減点: `fundamental_model.meta.json` の `data_version=null` (持ち越し)、単一 commit で時系列証跡が弱い。
- **過適合監視 / 分割ガード / 統合: 3/5** — `SPLIT_DIVERGENCE` は等値 assert を **「30 文字超の理由文字列があれば通過」に緩めた**もの。具体的事故 — 新 train (2022-2024) で学習した booster を `DATA_PERIODS["test"]` (2024-2025、filter_sweep / bias_scan の既定) で評価すると 2024 が in-sample — を落とす実行時チェックが無い (grep で `meta["train"]` 参照ゼロ)。モデル meta に学習窓は記録済みなので **1 assert で強制可能**。検証窓半減: 主要仮説の検出力には無関係だが早期停止の分散は約 √2 倍。

## 依頼 5 点への回答

1. **2026 を見ずに決めたか** — **反証できず、間接証拠は支持**。代替案の成果物ゼロ、`dirty_paths` の順序が整合、`run_index=1`。ただし squash 1 commit で時系列証跡は無く、2026 の **特徴分布**は判断材料として見ている。閾値 4 歳 / 3 歳はドメイン根拠で成績依存の痕跡なし。
2. **凍結** — **成立**。
3. **同日リーク検査は本物か** — **概ね本物、ただし部分的**。7 項目中 6 項目一致、1 項目 (勝者未確定レース) が乖離 — 今回の窓では該当 0 行。調教師のみなので父・騎手・馬の独立再計数を足すこと。
4. **SPLIT_DIVERGENCE** — **guard は緩んだ**。宣言は事故を防がない。防ぐには評価側で booster meta の窓と評価窓の非重複を assert する必要がある (現状ゼロ)。
5. **検証窓半減** — 主要仮説の検出力は不変。影響は早期停止の分散と検証 ΔLogLoss の CI 幅 (~×1.41) で、4B はその CI を出していない。

## 停止条件チェック

- [x] git_sha / git_dirty / dirty_paths / run_index / label 記録あり。fundamental meta のみ data_version=null
- [x] baseline paired 比較成立 (凍結 baseline と比較)
- [ ] market_snapshot counts / payout 欠損 — N/A
- [x] 期間ズレ・コードパスズレ・事後の窓変更なし
- [x] 前回宣言の執行: 検出力節あり / skew 訂正あり、結論文は「確定市場に対し確認できなかった / T−10 未検定」に二分 → **FAIL 降格なし**

## 反証の試み

- 「凍結され上書きされていない」→ **成立**
- 「同日リーク 0 件は独立再計数による」→ **今回は成立、将来窓で偽陰性の余地**
- 「土台の非定常性は消えた」→ 新フラグの発火率 24.3%→1.4% の単調ドリフト → **不成立**
- 「2021 burn-in で 2022 開始時点の履歴は 1 年確保」→ 人的ローリングは成立、**馬の通算カウンタは 2022 行の 41% が打ち切り** → **部分的に不成立**
- 「性能を見て修正を選んでいない」→ 痕跡なし → **反証不能**

## 主な改善提案

1. **`h_history_truncated` の扱いを 2021-2025 だけで決める** — 残すなら「学習 24%・評価 1-4% の既知 skew」として PREREG に明記、外すなら馬カウンタも `*_365` に置換
2. **学習窓と評価窓の非重複を実行時に強制** — Booster 読込直後に `.meta.json` の `train` / `validation` と評価窓の重なりを assert
3. **監査の網羅性** — 父・騎手・馬カウンタにも拡張し、`finished_races` 条件を SQL に追加。学習窓からも標本を取る

## 前回からの差分

- 前回 3.6 / HOLD → 今回 3.4 / HOLD (−0.2)。較正 4→3 (検証 Δ の CI 欠落・旧 N 引用)。
- **執行確認**: 検出力の事前記載 → 執行。skew 方向訂正 → 執行。`_samples` CSV → 執行。`run_index` 引数化 → 執行。**未執行 (2 回目)**: 消費窓台帳の集約、fundamental meta の `data_version`。
- 宣言: 次回 (Group A 着手 commit) で **`h_history_truncated` の skew が実測付きで記載されていない、または学習窓と評価窓の重なりを落とす実行時 assert が無いまま新 train モデルを `DATA_PERIODS` 系に接続していたら FAIL に降格**。Group A の PREREG に群ごとの数値検出力が埋まっていなければ HOLD 固定を継続。
